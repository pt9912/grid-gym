# ADR-Index — grid-gym

Lebende Uebersicht ueber alle ADRs, ihren Status und die
Vorrang-/Schaerfungs-Beziehungen zwischen ihnen. Diese Datei ist
**kein** ADR-Entscheidungstext — sie ist eine Service-Notiz fuer
Reviewer, damit Drift zwischen Accepted-Texten (die per
`ADR 0006 §3` immutable sind) und nachgelagerten Folge-ADRs
sichtbar bleibt.

Reihenfolge: aufsteigend nach ADR-Nummer. Eine ADR mit Schaerfung
durch eine Folge-ADR traegt eine Spalte „Schaerfungen" mit
Verweisen — die Folge-ADR ist verbindlich, der Original-Text
historisch.

---

## Aktive ADRs

| ADR  | Titel                                                           | Status      | Datum       | Schaerfungen / Folge-ADRs                                                                                          |
| ---- | --------------------------------------------------------------- | ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| 0001 | [Dokumentations- und Planungsstruktur](0001-documentation-and-planning-structure.md) | Accepted    | 2026-05-13  | —                                                                                                                  |
| 0002 | [Sprach- und Build-Stack](0002-language-and-build-stack.md)     | Accepted    | 2026-05-14  | [`ADR 0008`](0008-enum-as-domain-frozen-form.md) erweitert §A-1 [`AC-DOMAIN-FROZEN`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert) um Enum-Subklasse als dritte Form; [`ADR 0029`](0029-no-coverage-pragma-contract.md) ergaenzt §A-1 um den 11. `tools/arch_check.py`-Contract `AC-NO-COVERAGE-PRAGMA` (Schaerfung-ohne-Supersedes per ADR 0011, M3-Welle-5-Hygiene-Folge); [`ADR 0046`](0046-multi-python-test-stage-pattern.md) erweitert das Multi-Stage-Dockerfile-Build-Pattern additiv um eine Library-Compat-Test-Stage auf einer gepinnten, von der `base`-Stage abweichenden Python-Version (Schaerfung-ohne-Supersedes per ADR 0011, M6-Welle-6; §2 Sprach-Floor `>=3.13` bleibt unangetastet). |
| 0003 | [ADR-Lifecycle](0003-adr-lifecycle.md)                          | Superseded  | 2026-05-13  | Abgeloest durch [`ADR 0006`](0006-adr-lifecycle-superseding-and-process-corrections.md)                            |
| 0004 | [Kennungs-basierte Querverweise](0004-identifier-based-cross-references.md) | Accepted    | 2026-05-13  | —                                                                                                                  |
| 0005 | [Type-Check-Gate (`mypy --strict`)](0005-type-check-gate.md)    | Accepted    | 2026-05-15  | —                                                                                                                  |
| 0006 | [ADR-Lifecycle, Superseding und Prozess-Korrekturen](0006-adr-lifecycle-superseding-and-process-corrections.md) | Accepted    | 2026-05-15  | **§3-Aenderungsregeln** geschaerft durch [`ADR 0011`](0011-schaerfung-ohne-abloesung.md): „Schaerfung ohne Supersedes" als zulaessige Folge-ADR-Form. Zusaetzlich geschaerft durch [`ADR 0028`](0028-link-maintenance-accepted-adr-bezug.md): Pfad-Updates an Markdown-Hyperlink-URLs in `Bezug:`-Linien sind keine fachliche Aenderung (Forwarder-Stub-Pattern abgeloest durch direkte Pfad-Pflege). |
| 0007 | [`RandomPort`-Implementierung](0007-random-port.md)             | Accepted    | 2026-05-15  | **§5.2-Snapshot-Schema** abgeloest durch [`ADR 0009`](0009-randomport-snapshot-schema-rng-version.md): `state`-Feld → `rng_version` + `rng_state`. **§5.1-Protocol-Erweiterung** durch [`ADR 0010`](0010-randomport-snapshot-as-mapping.md): zusaetzliche Methode `snapshot_as_mapping()`. Beide reine Erweiterungen, kein Supersedes. |
| 0008 | [Enum-Subklassen als AC-DOMAIN-FROZEN-Form](0008-enum-as-domain-frozen-form.md) | Provisional | 2026-05-17  | Erweitert `ADR 0002 §A-1` [`AC-DOMAIN-FROZEN`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert); Acceptance synchron zur M1-Welle-1-PR-Mergung                          |
| 0009 | [`RandomPort`-Snapshot-Schema](0009-randomport-snapshot-schema-rng-version.md) | Accepted    | 2026-05-17  | Erweitert `ADR 0007 §5.2`; reine Erweiterung, kein Supersedes                                                       |
| 0010 | [`RandomPort.snapshot_as_mapping`](0010-randomport-snapshot-as-mapping.md) | Accepted    | 2026-05-17  | Erweitert `ADR 0007 §5.1` + `ADR 0009`: Composition-API fuer `SnapshotEnvelope`. Single-Source-of-Truth `_build_payload()`. Reine Erweiterung, kein Supersedes. |
| 0011 | [Schaerfung durch parallele ADR ohne Supersedes](0011-schaerfung-ohne-abloesung.md) | Accepted    | 2026-05-17  | Schaerft `ADR 0006 §3`: dokumentiert die „Schaerfung ohne Supersedes"-Form, die ADR 0008/0009/0010 bereits implizit nutzen. Self-bootstrap (ist selbst dieses Muster). |
| 0012 | [API + Simulation als zwei Prozesse](0012-api-simulation-two-processes.md) | Accepted    | 2026-05-17  | Schliesst `GG-AR-OPEN-002` (spec/architecture.md §19): api + simulation als zwei Prozesse, Postgres als Persistenz-Bus. Welle 6c hat den Pattern bereits implementiert; ADR formalisiert nachtraeglich. |
| 0013 | [`DeviceModel`-Protocol als Core-internes Protocol](0013-device-model-protocol.md) | Accepted    | 2026-05-18  | `DeviceModel`-Vertrag fuer M2-Geraete (`GG-DEV-001..003`). §2.4 `from_snapshot`-Pflicht schaerft durch [`ADR 0014`](0014-battery-snapshot-schema.md) (Battery), [`ADR 0016`](0016-pv-load-device-pattern.md) (PV+Load), [`ADR 0017`](0017-grid-connection-device-pattern.md) (GridConnection), [`ADR 0018`](0018-smart-meter-device-pattern.md) (SmartMeter) — alle reine Erweiterungen je Geraet, kein Supersedes. |
| 0014 | [Battery-Snapshot-Schema + Command-Surface (M2 Welle 2)](0014-battery-snapshot-schema.md) | Accepted    | 2026-05-18  | Vorlage fuer das Geraete-Snapshot-/Command-Pattern. Schaerft `ADR 0013 §2.4` fuer den Battery-spezifischen Snapshot-Vertrag (`Schaerfung ohne Supersedes`-Pattern, ADR 0011). §§3/7 cross-referenziert durch [`ADR 0016`](0016-pv-load-device-pattern.md) (`set_mode` ist Welle-5+-Material). |
| 0015 | [TickLoop-Snapshot-v2 im SnapshotEnvelope-Pattern (M2 Welle 6a)](0015-snapshot-envelope-v2.md) | Accepted    | 2026-05-20  | Welle-6a-TickLoop-Snapshot-Bump: `TickLoop.snapshot()["version"]` von `1` (M1-Welle-4-Stand) auf `2`; der generische `SnapshotEnvelope`-Konstruktor bleibt tolerant. Strukturierender Bruch — der Inhalts-Vertrag des `sub_snapshots`-Mapping erweitert sich um `devices.<device_type>.<device_id>` x 5 und `grid_model`. v1-Read in M2-Code-Pfaden wirft typisierten `TickLoopSnapshotVersionError` (mit M6-`GG-PERSIST-*`-Pointer in der Message). Unbekannte Sub-Snapshot-Keys werden toleriert (Forward-Compat fuer Welle 7+ `agents.*` o.ae.). Schreib-Pfad-Exception `TickLoopUnknownDeviceTypeError` fuer fehlende `_DEVICE_TYPE_BY_CLASS_NAME`-Eintraege. §4-Forward-Pointer fuer Welle-7+/M3-`device_type`-Protocol-Property. Status-Uebergang: `Proposed → Provisional` (Welle 6a-Merge) → `Accepted` (Welle-6c-Closure `c31052c`, MVP-Demo exerziert v1→v2 + typisierten Error end-to-end). |
| 0016 | [PV + Load Generation/Consumption-Device-Pattern (M2 Welle 3)](0016-pv-load-device-pattern.md) | Accepted    | 2026-05-18  | Gemeinsame ADR fuer PV (`GG-DEV-011`) und Load (`GG-DEV-013`). Spiegelt das ADR-0014-Pattern in einfacherer Form (kein SOC / Ramp / Wirkungsgrad). §2.2 Sign-Konvention bindet [`ADR 0017`](0017-grid-connection-device-pattern.md) §2.2 (gleiche Netzbilanz-Formel) und [`ADR 0018`](0018-smart-meter-device-pattern.md) §2.4 (Aggregation respektiert Quellen-Sign). |
| 0017 | [GridConnection-Anschlusspunkt-Pattern (M2 Welle 4a)](0017-grid-connection-device-pattern.md) | Accepted    | 2026-05-19  | Welle-4a-Geraet `GG-DEV-012`. Stateful Anschlusspunkt mit kumulativem `import_kwh`/`export_kwh`. Sign-Konvention `power_kw > 0 = Import` schliesst die Welle-5-Netzbilanz-Formel aus [`ADR 0016`](0016-pv-load-device-pattern.md) §2.2. Separate ADR statt geteilt mit [`ADR 0018`](0018-smart-meter-device-pattern.md) (verschiedene State-Modelle). §§2.4/2.5 geschaerft durch Welle-4a-Review-Folge (Commit `1ed976a`: IGNORED-Klarstellung + Decimal-Praezisions-Note). Status-Uebergang: `Proposed → Provisional` (Welle 4a-Merge) → `Accepted` (Welle-4-Closure). |
| 0018 | [SmartMeter-Aggregator-Pattern (M2 Welle 4b)](0018-smart-meter-device-pattern.md) | Accepted    | 2026-05-19  | Welle-4b-Geraet `GG-DEV-014`. Stateless Aggregator ueber `aggregate_device_ids: tuple[str, ...]` via neuem `attach_sources(...)`-Lifecycle-Hook (Analogie zu Welle-3-Review-M-6-`attach_random`). Snapshot persistiert **keine** Aggregat-Werte (derived). Schwester-ADR zu [`ADR 0017`](0017-grid-connection-device-pattern.md). §2.2 geschaerft durch Welle-4b-Review-Folge (Commit `1093b2c`: `aggregate_metric_name`-Pflicht auf `"power_kw"` in Welle 4b; Welle 5+ aktiviert Forward-Looking). Status-Uebergang: `Proposed → Accepted` direkt (Welle-4-Closure). |
| 0019 | [Grid-Model-Bilanz-Pattern (M2 Welle 5a)](0019-grid-model-bilanz-pattern.md) | Accepted    | 2026-05-19  | Welle-5a-Modul `hexagon/core/grid_model/` (`bilanz.py` + `config.py` + `snapshot.py`). Proportionales Frequenz/Spannungs-Modell `f = f_nom + k_f * imbalance`, `v = v_nom + k_v * imbalance` mit Safety-Clamps. **Kein DeviceModel** — Single-Instance-System-Modell, `grid_model` als Snapshot-Sub-Key (kein `devices.<id>`). Imbalance-Berechnung schliesst die Welle-5-Netzbilanz aus [`ADR 0016`](0016-pv-load-device-pattern.md) §2.2 und [`ADR 0017`](0017-grid-connection-device-pattern.md) §2.2; GridConnection wird in Welle 6 als Bilanz-Schluss-Variable verdrahtet. §§3/5/6 geschaerft durch 3 Pre-Implementation-Runden + Welle-5a-Review-Folge. Welle-5b-Lieferung erweitert das Snapshot-Layout v1→v2 mit Backward-Compat (ADR 0020 ohne Supersedes). Status-Uebergang: `Proposed → Provisional` (Welle 5a-Merge) → `Accepted` (Welle-5-Closure mit ADR 0020). |
| 0020 | [Load-Profile + Load-Event Pattern (M2 Welle 5b)](0020-load-profile-and-event-pattern.md) | Accepted    | 2026-05-19  | Welle-5b-Erweiterung von `hexagon/core/grid_model/` um `loads.py` (`LoadEvent` + `LoadProfile` + pure CSV/JSON-Parser). Schwester-ADR zu [`ADR 0019`](0019-grid-model-bilanz-pattern.md): Welle 5a ist Physik, Welle 5b ist Daten-/I/O-Integration. `GridModelSnapshot` bumpt v1→v2 mit Backward-Compat-Lesepfad (v1-Snapshot ohne LoadEvents/Profiles bleibt roundtrip-faehig). Liefert `GG-GRID-003` (Lasten konstant/Zeitreihe/Event) + `GG-GRID-004` (Lastspruenge). Datei-I/O ist Adapter-Verantwortung (`GG-AR-TABU-002`); §§2.1/2.2/2.3/2.4/2.5/2.6 geschaerft durch 1 Pre-Implementation-Runde + Welle-5b-Review-Folge (Commits `53a4f91` + `47c054a`). M3-Driftrisiko gegenueber Replay-Source-Pfad explizit dokumentiert (M3 baut auf 5b, repliziert nicht). Status-Uebergang: `Proposed → Accepted` direkt (Welle-5-Closure). |
| 0021 | [Scenario-Loader + TickLoop-Event-Wiring + GridConnection-Auto-Schluss (M2 Welle 6b)](0021-scenario-loader-and-tick-loop-event-wiring.md) | Accepted    | 2026-05-20  | Welle-6b-Builder-/Wiring-ADR: `hexagon/core/scenario/loader.py` erweitert um `build_devices(...)` (Factory-Dispatch nach `ScenarioDevice.type`), `build_tick_loop(...)` (produktiver TickLoop-Builder), und `parse_load_events`/`parse_load_profiles`-Funktionen (plus internem `_parse_grid_model_config`). Validator faengt die drei optionalen Top-Level-Sektionen (`grid_model`, `load_events`, `load_profiles`) typisiert ab. TickLoop erhaelt einen Vor-Tick-Block, der LoadProfiles (Baseline) → LoadEvents (Overlay) → GridConnection-Auto-Schluss (`-pre_grid_residual_kw`) auf Devices anwendet (Split-Iteration, half-open Event-Window mit `tick_start_ms`). SmartMeter-`attach_sources`-Aufruf ist Scenario-Loader-Verantwortung nach Device-Konstruktion. Neue typed Exceptions `ScenarioUnknownDeviceTypeError` + `ScenarioMissingSourceDeviceError` + `ScenarioInvalidLoadTargetError`. Plugin-Registry fuer externe DeviceModel-Klassen bleibt M5/M6-out-of-scope. Welle-6b-Review-Folge (Commit `0f1c597`) hat 14 Findings (4 High, 7 Medium, 3 Low, 1 Niedrig) ohne Supersede-ADR adressiert (Schaerfung-Pattern); H-4 Validator-Erweiterung + parse-Helfer schliesst den YAML→TickLoop-Pfad. Status-Uebergang: `Proposed → Provisional` (Welle-6b-Merge, Commit `0f1c597`) → `Accepted` (Welle-6c-Closure `c31052c`, MVP-Demo exerziert Builder + Vor-Tick-Block produktiv ueber ≥ 100 Ticks + Permutations-Property-Test). |
| 0022 | [Fault-Injection-Protocol + Scenario-Validator-Härtung (M3 Welle 1)](0022-fault-injection-protocol.md) | Provisional | 2026-05-20  | M3-Welle-1-Erstwurf: `FaultInjectableDevice(DeviceModel)` Sub-Protocol unter `hexagon/core/faults/_protocol.py` (per ADR 0013 §2.8 — keine Erweiterung der Base-`DeviceModel`-Surface, sondern Closed-Set-Sub-Protocol mit `inject_fault(fault_type, payload)`). Neuer Driven-Port `FaultPort` unter `ports/driven/fault.py` mit `apply_active_faults(devices, context) -> None` (Orchestrierungs-Hook, nicht Per-Device-Hook; Welle-1-Port-Surface ist structural `Sequence[object]` wegen [`AC-PORTS-NO-OUT`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert) — Welle-2-Adapter typisiert intern strenger). Scenario-Validator-Haertung: `_assert_fault_list` prueft Target-Existenz analog `_assert_event_list` (neuer typed `ScenarioUnknownFaultTargetError`); KEINE Type-Whitelist, KEINE semantische Validation (Welle-2-Material). TickLoop-Hook im Vor-Tick-Block nach `_consume_load_inputs_into`, vor erster `_run_device_iteration`; neuer Konstruktor-Kwarg `fault_port: FaultPort \| None = None` (keyword-only, kein produktiver NullFaultPort — Hook skippt bei None). Snapshot-Schema bleibt v2 in Welle 1; Bump v2→v3 wird in Welle 2 oder M6 entschieden. Welle 2 liefert konkrete Adapter (`BatteryFaultAdapter`, `GridFaultAdapter`) + Fault-Typen (`cell_failure`, `voltage_drop`) + Recovery-Logik. Status-Uebergang: `Proposed → Provisional` (M3-Welle-1-Merge `79bb50a`, 773 Unit-Tests + 11 neue Fault-Tests). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.4 (Recovery-Semantik) wird durch [`ADR 0025`](0025-fault-recovery-pattern.md) erweitert (Schaerfung-ohne-Supersedes, ADR 0011-Pattern). |
| 0023 | [Multi-Agent-Bus + Agent-Protocol (M3 Welle 3)](0023-agent-bus-protocol.md) | Provisional | 2026-05-21  | M3-Welle-3-Erstwurf: `Agent`-Sub-Protocol unter `hexagon/core/agents/_protocol.py` (eigenstaendig, **nicht** `DeviceModel`-erbend — Agents stehen neben Geraeten, nicht auf ihnen; ADR 0013 §2.8-konform). `AgentMessageBus` als **Core-Klasse** unter `hexagon/core/agents/bus.py` (Pattern-Drift gegen ADR 0022 `FaultPort`: Architektur §14 schreibt eigenes Kernmodul + AgentMessageBus vor, Architektur §4.2 listet keinen AgentBus-Port, State-/Boundary-Argument — Driven-Ports sind Adapter-Boundary, AgentBus hat keine externe Boundary; `MersenneTwisterRandomPort` ist Gegenbeleg: Port + stateful + externe PRNG-Library, Welle-3-Review-Folge M-2 schaerft die urspruengliche AC-PORTS-NO-OUT-Argumentation, Test-Isolierung via Sub-Protocol). Status-Uebergang: `Proposed → Provisional` (M3-Welle-3-C2-Merge `4fa122d` + Review-Folge `d6f66fc`, 879 Unit-Tests + 14 Integration-Tests + A-1-Gates gruen ohne Override). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen** (Welle-3-Review-Folge, alle Schaerfung-ohne-Supersede per ADR 0011): H-1 (`AgentMessage.payload`-MappingProxyType-Versprechen gestrichen — Konsistenz mit ScenarioFault/Command/Event-Pattern; Domain-weiter Wrap waere Welle-4-Refactor); M-1 (`agent_message.py` aus `CRITICAL_COV_TARGETS` gestrichen — Dockerfile-Stage prueft `[ ! -d "${target}" ]`, Coverage erfasst das Modul trotzdem via `core/agents/bus.py`-Import); M-2 (AC-PORTS-NO-OUT-Argument durch State-/Boundary-Argument ersetzt — `MersenneTwisterRandomPort` als Gegenbeleg fuer "Port + stateful"); M-3 (Sub-Random-Stream-Konvention `agent-{agent_id}` nach Welle 4 verschoben, weil Welle-3-Bus keinen RandomPort-Kwarg hat); M-4 (Welle-4-Pflicht-Eviction-Spec ergaenzt — `consume_for(receiver)` mit Per-Receiver-Watermark oder `evict_before(simulation_time)`; Welle-3-Foundation-Buffer waechst sonst unbegrenzt). Plus L-1..L-4 als Code-Schaerfungen (`_set_agents_for_testing`-Helper am TickLoop mit Agent-ID-Eindeutigkeits-Check; `AgentBusInvalidSequenceError` fuer `publish(sequence < -1)`; `AgentBusInvalidReceiverError` fuer `drain_for("*")`; `AgentBusError`-Welle-3-Surface-Klarstellung). | `AgentMessage`-frozen-dataclass unter `hexagon/core/domain/agent_message.py` mit `GG-AGENT-004`-Pflicht-Feldern. TickLoop-Hook zwischen Schritt D und E (Architektur §6 Schritt 7); Agent-Commands gehen in den Scheduler und werden in der **naechsten** Tick wirksam ([`GG-AGENT-008`](../../../spec/lastenheft.md#gg-agent-008) Commit-Reihenfolge-Invariante). Neuer Konstruktor-Kwarg `agent_bus: AgentMessageBus \| None = None` (keyword-only, kein produktiver NullBus — Hook skippt bei None); `build_tick_loop`-Builder-Symmetrie analog ADR 0021 §2.4. Observability-Vorgriff-Verbot: AgentBus/Agent injizieren **keine** LogPort/MetricsPort/TracePort in Welle 3 (ADR 0024 entscheidet Wiring in Welle 5/6). Trigger 011 (Sub-Seed-Wortbreite) bleibt in `open/` — Welle-3-Skala (< 100 Agents) erreicht Aktivierungs-Schwelle (`> 10⁶ Sub-Ports`) nicht. Snapshot-Vertrag: AgentMessageBus haelt eigene `snapshot()`-Surface, wird aber in Welle 3 nicht in `TickLoop.snapshot()` eingehaengt; Welle 4 fuegt Sub-Snapshot-Slot `agent_bus` + `agents.<type>.<id>` additiv per ADR 0015 §2.3 ein. Welle 4 liefert konkrete `Agent`-Implementer (`RuleBasedAgent` o. ae.), Agent-Registry, Decision-Logik, `agents`-Top-Level-Block im Scenario-Schema. Status-Uebergang: `Proposed → Provisional` mit M3-Welle-3-C2-Merge (TBD). `Accepted` mit M3-Welle-7-Closure. |
| 0024 | [Observability-Port-Trio: LogPort + MetricsPort + TracePort (M3 Welle 5)](0024-observability-port-trio.md) | Provisional | 2026-05-23  | M3-Welle-5-Erstwurf: Driven-Port-Trio `LogPort`/`MetricsPort`/`TracePort` als `Protocol` (`@runtime_checkable`, stateless, keine Default-Methoden, **keine** OTLP/SDK-Typen im Port-Layer — Core bleibt OTLP-frei). `TracePort` mit internem `SpanContext`-frozen-dataclass (`trace_id`/`span_id`/`parent_span_id` String-basiert); `start_span`/`end_span`/`record_event` mit Pflicht-`SpanContext`-Param + No-Op-Fallback bei `None` als Adapter-Robustheit. Drei Null-Adapter (`adapters/driven/observability_null/`) mit Default-`call_count`+`last_call`-Surface und opt-in `record_calls=True` fuer `call_records`+`clear_calls()` (loest M3-welle-5.md §7 R-2 — Coverage ohne opt-in). Additive Hook-Verdrahtung in TickLoop (Tick-Telemetrie + Logs) / Agent (Decision-Trail) / Fault (Span-Wrap + Audit) — ADR 0022/0023/0026 Schritt-/Atomizitaets-Vertraege unangetastet; `log_port`/`metrics_port`/`trace_port`-Kwargs konstruktor-symmetrisch zu ADR 0022/0023 mit Default `None` (Hook skippt). Loest [`ADR 0023`](0023-agent-bus-protocol.md) §2.6 Observability-Vorgriff-Verbot auf. Trigger 006 (`--strict-bytes`) explizit deferred auf Welle 6 (kein Bytes-Pfad in Welle 5). Status-Uebergang: `Proposed → Provisional` (M3-Welle-5-C2-Merge `718c177`, 1023 Unit-Tests + 19 Integration-Tests, `make fullbuild` cache-frei gruen ohne Override, Coverage 95.55%). `Accepted` mit M3-Welle-7-Closure nach Welle-6-OTLP-Compose-Smoke-Verifikation. **Hygiene-Folge** (`9ae2376`): [`ADR 0029`](0029-no-coverage-pragma-contract.md) `Accepted` (Schaerfung-ohne-Supersede von ADR 0002 §A-1; 11. arch_check-Contract `AC-NO-COVERAGE-PRAGMA`). **Welle-5-Review-Folge** (2026-05-23, Vor-`Accepted`-Schliff per ADR 0006 §4): §1 N-3 (Pre-Tick-Hook-Ref `ADR 0022 §2.5 → §2.4`); §2.6 M-1 (Log-/`tick_duration_ms`-Hooks als Welle-6-Material qualifiziert — Welle-5-Minimum ist Span-Wrap; `AC-NO-TIME` blockiert Wall-Clock im Core); §4.4 M-2 (neu — Welle-6-Forward-Pointer: Sentinel-Pattern fuer `scenario.observability` analog Welle-4b-F-1, Trace-ID-Determinismus via `RandomPort.sub_port` analog Welle-3-M-3, Vertragsschnitt fuer `SpanContext`+Port-Signaturen analog Welle-4b-F-2, plus L-2/N-1/N-2 als Welle-6-Folgearbeit-Bullets); `Bezug:`-Liste L-3 (`ADR 0011` ergaenzt). Code-Inline-Fixes: H-1 (tick_span im try/finally), L-1 (tick_end-Log vor Span-Close), M-3 (Makefile-Help-Text 16→17), L-4 (Test-Asserts `is → ==`). **Welle-6-C1.2-Schaerfung** (2026-05-24, Schaerfung-ohne-Supersede per ADR 0011): §4.5 ergaenzt um acht normative Decisions fuer den Welle-6-OTLP-Adapter — L-2 (Provider-Lifecycle: lazy Provider-Init im Adapter-Konstruktor, OTel-globale `set_logger_provider`/`set_meter_provider`/`set_tracer_provider` werden im `build_otlp_adapters`-Factory gesetzt; `flush_and_shutdown`-Helper fuer geordneten Teardown), N-1 (resource semconv: `service.name`/`service.version`/`deployment.environment` + `service.instance.id` als Pflicht-Felder via `OtlpAdapterConfig`), N-2 (`scenario.observability`-Sentinel-Pattern fuer fehlende Telemetrie-Konfiguration analog Welle-4b-F-1 — `None`-Default propagiert), Sentinel (`scenario.observability is None` → `NullLogAdapter`+`NullMetricsAdapter`+`NullTraceAdapter` ohne Init-Side-Effects), Trace-ID-Determinismus (Span-IDs via `RandomPort.sub_port("trace")` deterministisch in Tests, Production-Pfad delegiert an OTel-SDK), D-4 (Decision-Vertragsschnitt `SpanContext`+Port-Signaturen unveraendert — Adapter ist additiv ohne Core-API-Bruch), gRPC-Pinning (`opentelemetry-exporter-otlp-proto-grpc` als einzig zulaessiger Exporter in Welle 6; `proto-http` als Welle-7+-Material), Smoke-Determinismus (Compose-Smoke-Verifikation in Welle-6-C3 muss `≥1 Span + ≥1 Metric + ≥1 LogRecord` pro Tick-Run beobachten). Neuer 12. arch_check-Contract `AC-OTLP-ADAPTER-NO-TIME` (Schaerfung-ohne-Supersede von ADR 0002 §A-1): verbietet `time.time`/`time.monotonic`/`datetime.now` etc. in `src/grid_gym/adapters/driven/telemetry_otlp/**` (Adapter-interner Code muss Wall-Clock durch `ClockPort` beziehen — sonst Drift gegen TickLoop-Sim-Zeit). |
| 0025 | [Fault-Recovery-Pattern (M3 Welle 2)](0025-fault-recovery-pattern.md)                          | Provisional | 2026-05-20  | M3-Welle-2-Erstwurf: Recovery-Engine fuer Faults. Zwei Recovery-Modi in Welle 2 produktiv: `auto-recover-after-N-ticks` (Default, Adapter zaehlt `remaining_ticks` herunter und clearet Device-Flag nach Window-Ende) + `manual-via-command` (explizites `manual-recover-fault`-Command mit Pflicht-Payload `fault_id` + `target_device_id`, optional `correlation_id`). `permanent`-Modus verschoben auf Welle 3+/M6 (braucht Lauf-uebergreifende Persistenz). Recovery-State-Lokalisation: **Adapter haelt Scheduling** (`_active_faults: dict[(fault_id, target_id), int]`), **Device haelt nur Physik-Flag** (`_cell_failure_active: bool`, `_voltage_drop_active: bool`). `inject_fault`-Idempotenz: Adapter ruft Device nur beim Uebergang inactive→active oder active→inactive (nicht bei unveraendertem Zustand). Recovery-Window-Vertrag: half-open `[start, end)` analog Welle-6b-LoadEvent (ADR 0021 §2.5). Snapshot-Erweiterung: Battery + GridConnection Sub-Snapshots additiv um `fault_state`-Block (ADR 0015 §2.3 erlaubt das ohne v2→v3-Bump; ADR 0014/0017 bleiben `Accepted`). Recovery-Telemetry verschoben auf Welle 5/6 (Observability). Schaerft ADR 0022 §2.4, kein Supersede (ADR 0011-Pattern). Status-Uebergang: `Proposed → Provisional` (M3-Welle-2-Closure `91d44e2`, 840 Unit-Tests + 14 Integration-Tests + A-1-Gates gruen). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen** (Welle-2-Closure-Folge): Property-Tests + Fault-Demo-Integration + Composite-FaultPort-Test-Helper (`tests/integration/_fault_composite.py`, Welle-3-Promote-TODO); Symmetrie-Notiz `build_tick_loop(fault_port=)` in ADR 0021 §2.4 + ADR 0022 §2.5 ergaenzt. |
| 0026 | [Agent-Drain + Registry + Snapshot + Lifecycle (M3 Welle 4a)](0026-agent-drain-registry-pattern.md) | Provisional | 2026-05-21  | M3-Welle-4a-Erstwurf: Schwester-ADR zu ADR 0023 (Welle-3-Foundation); Pattern-Pendant zu ADR 0025 (Welle-2-Konkretisierung). Schaerft ADR 0023 §6 verbindliche Welle-4-Konsequenzen ohne Supersede (ADR 0011-Pattern). Sechs Entscheidungen: §2.1 Drain-Pfad als Pre-Tick-Schritt **A0v** (Target-Validierung vor `clock.advance`/`scheduler.pop_due`) + **A0a** (`apply_command`-Apply nach Clock, vor Schritt A) — Atomizitaets-Vertrag bei `AgentInvalidCommandTargetError`; Agent-Commands auf GridConnection-IDs ergaenzen `manual_override_grid_ids` (manueller Auto-Close-Override); LoadDevice-Baseline gewinnt im selben Tick. §2.2 Registry-API: TickLoop-Konstruktor-Kwarg `agents: tuple[Agent, ...] = ()` + **Auto-Bus-Regel** (`agents != () and agent_bus is None` → leerer Bus, damit registrierte Agents nicht still als No-op enden) + Agent-ID-Eindeutigkeit (`AgentDuplicateIdError`); `build_tick_loop`-Symmetrie + `GridModelBilanz`-Overlay-Verdrahtung. §2.3 Lifecycle: `_attach_agents()` mit `set_run_id` + optionalem `_RandomAttachableAgent`-Sub-Protocol (`@runtime_checkable`); Sub-Random-Stream-Konvention `agent-{agent_id}` produktiv (Welle-3-Review-Folge M-3 geschlossen). §2.4 Bus-Eviction: `consume_for(receiver)` als **Direct-Inbox-destruktive** Drain-Variante; Broadcasts bleiben nicht-destruktiv (Welle-3-Review-Folge M-4 teilweise geschlossen; Broadcast-Watermark bleibt Folge-Slice). §2.5 Error-Family-Trennung: `AgentRegistryError`/`AgentDuplicateIdError` (Konstruktor), `AgentCommandDrainError(TickLoopError)`/`AgentInvalidCommandTargetError` (Schritt A0v), fuenf `TickLoopAgentSnapshot*Error` (Resume-Match); malformed `agent_bus`-Sub-Snapshots delegieren an `AgentBusSnapshot*`-Errors unwrapped. §2.6 Snapshot-Vertrag: `agent_bus` + `pending_agent_commands` als Sub-Snapshots; `TickLoop.from_snapshot(..., devices=..., agents=...)` mit Runtime-Dependency-Kwargs + Resume-Match-Checks (Device-ID/Typ/State, GridModel-State, LoadOverlay-State falls `grid_model`-Sub-Snapshot vorhanden, Pending-Command-Target-Validitaet); Auto-Bus-Praezedenz bei alten Snapshots ohne `agent_bus`; `CommandResult` als String-Roundtrip. **Konkrete Agent-Instanz-Snapshots (`agents.<type>.<id>`) bleiben Welle 4b**. Welle 4a entfernt Welle-3-`_set_agents_for_testing(...)`-Helper. Status-Uebergang: `Proposed → Provisional` mit M3-Welle-4a-C3-Closure `556baaf` (2026-05-21). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.6 Resume-Match-Checks geschaerft durch Welle-4a-Review-Folge (Commit `38272f6`): `_assert_device_resume_match` **bidirektional** (Subset-Restores werfen `TickLoopAgentSnapshotDeviceMismatchError`), nested-Equality via `canonical_json`-Bytes (kein False-Positive nach tuple/list-Roundtrip). §6 Welle-4b-Konsequenzen konkret geschaerft durch [`ADR 0027`](0027-rule-based-agent-scenario-pattern.md) (Schaerfung-ohne-Supersedes, ADR 0011-Pattern). |
| 0027 | [RuleBasedAgent + Scenario-Agents-Block-Pattern (M3 Welle 4b)](0027-rule-based-agent-scenario-pattern.md) | Provisional | 2026-05-22  | M3-Welle-4b-Erstwurf: Pattern-Pendant zu ADR 0025 (Welle-2-Konkretisierung) und ADR 0026 (Welle-4a-Foundation). Schaerft ADR 0026 §6 verbindliche Welle-4b-Konsequenzen ohne Supersede (ADR 0011-Pattern). Fuenf Entscheidungen: §2.1 `agents`-Schema als **nested Mapping** mit `agent_id` als Key (Schema-eindeutige IDs, fachliche Map-Semantik statt geordnete Liste; Konsistenz-Bruch zu `devices`/`faults`/`events`/`load_events`/`load_profiles` als bewusste fachliche Entscheidung dokumentiert); Loader iteriert `sorted(agents.keys())`. §2.2 `ScenarioAgent`-Domain (frozen dataclass, analog `ScenarioDevice`) + `_assert_agent_list(...)`-Validator (Pattern parallel zu `_assert_fault_list`); `_build_agents(...)` Factory-Dispatch analog `build_devices` + neue typed Errors (`ScenarioUnknownAgentTypeError`, `ScenarioUnknownAgentTargetError` u. a.). §2.3 `RuleBasedAgent`-**Hybrid Decision-Surface**: Default-Pfad ist deterministische Threshold-Rules-Liste (first-match-wins, geordnete Tuple, snapshot-bar, scenario-spezifizierbar); Erweiterungs-Pfad ist optionaler `plugin`-Hook mit `_AGENT_PLUGIN_FACTORIES`-Registry (Welle 4b leer; konkrete Plugins sind Welle 4c+); Mutual Exclusivity Rules ODER Plugin (vermeidet Reihenfolge-Drift). Neues `AgentPlugin`-Sub-Protocol (`@runtime_checkable`) mit eigenem `snapshot`/`from_snapshot`-Vertrag. §2.4 `agents.<agent_type>.<agent_id>`-Sub-Snapshot-Layout additiv per ADR 0015 §2.3 (kein Schema-Bump); bidirektionaler Resume-Match-Check analog Welle-4a-Review-Folge. §2.5 Welle-4-Abschluss-Gate: `make fullbuild` ohne Override cache-frei gruen mit Agent-Demo-Szenario. §2.6 Demo-Szenario-Location `tests/integration/scenarios/agents_demo.yaml` (Konsistenz zu `mvp_demo.yaml`/`fault_demo.yaml`). Status-Uebergang: `Proposed → Provisional` mit M3-Welle-4b-C2-Merge `95979e5` (2026-05-22) + C2-Review-Folge `11b2ca9` + dep-audit-Fix `ac7b47f` (starlette 1.0.0→1.0.1). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.2 build_tick_loop-`agents`-Kwarg-Default per Sentinel-Pattern (`None` statt `()` — Welle-4b-Review-Folge F-1, expliziter agentenloser Override wird respektiert). §2.3 + §7 Plugin-Restore-Scope-Schnitt expliziter: Welle-4b rekonstruiert nur Rules + plugin_name (kein Plugin-State); konkrete Plugin-Restore-Surface ist Welle-4c+-Material (Welle-4b-Review-Folge F-2). |
| 0028 | [Link-Maintenance an Accepted-ADR-`Bezug:`-Linien](0028-link-maintenance-accepted-adr-bezug.md) | Accepted    | 2026-05-23  | Schaerft `ADR 0006 §3` (Schaerfung-ohne-Supersedes per ADR 0011): Pfad-Updates an Markdown-Hyperlink-URLs in `Bezug:`-Linien sind keine fachliche Aenderung, sofern Ziel inhaltlich identisch bleibt, Linktext unveraendert/pfadnamen-spiegelnd ist und die Verweis-Menge gleich bleibt. Begruendung: Stub-Forwarder-Pattern skaliert nicht und steht im Widerspruch zur Positionsfragilitaets-Ratio aus ADR 0004 §1. Erstanwendung: Cleanup von 5 Forwarder-Stubs (`planning/in-progress/M1-tick-loop-spine.md`, `planning/in-progress/M2-devices.md`, `planning/open/003-random-port-adr.md`, `planning/open/012-snapshot-composition.md`, `planning/next/M1-tick-loop-spine.md`) + Pfad-Fixups in 13 Accepted-ADRs (0007, 0008, 0009, 0010, 0013–0021). |
| 0029 | [AC-NO-COVERAGE-PRAGMA als 11. tools/arch_check.py-Contract](0029-no-coverage-pragma-contract.md) | Accepted    | 2026-05-23  | Schaerfung von `ADR 0002 §A-1` (Schaerfung-ohne-Supersedes per ADR 0011): elfter `tools/arch_check.py`-Contract `AC-NO-COVERAGE-PRAGMA` verbietet `# pragma: no cover`, `# pragma: no branch` und `# pragma: exclude file` in `src/grid_gym/**`. Coverage-Gate-Disziplin: Pragmas verstecken sonst unzureichend getestete Pfade vor den `make coverage-gate`-/`coverage-gate-critical`-Schwellen. Protocol-Stubs werden ueber den `^\\s*\\.\\.\\.\\s*$`-Pattern in `[tool.coverage.report].exclude_lines` global ausgenommen (statt Pragma-Annotation pro Stub); defensives Dead-Code wird per `typing.cast` nach vorgelagerter Type-Validation ersetzt; `if TYPE_CHECKING:`-Bloecke sind bereits ueber den Standard-`exclude_lines`-Eintrag abgedeckt. Erstanwendung: 32 Pragma-Vorkommen aus `src/grid_gym/**` entfernt (29 Protocol-Stubs, 2 defensive `isinstance`-Guards in `validator.py`, 1 unreachable `return ()` in `loader.py`, 1 redundante TYPE_CHECKING-Annotation in `domain/scenario.py`). 17 A-1-Contracts insgesamt (6 import-linter + 11 arch_check). |
| 0030 | [DeviceProtocolPort-Surface (M4 Welle 1)](0030-device-protocol-port-surface.md) | Accepted    | 2026-06-01  | M4-Welle-1-Erstwurf (`Proposed` 2026-05-26 `b840e7a`) + Review-Folge `ad3dff8` (3H+4M+5L) + H4-Korrektur `111c464` (Decision 3 auf Caller-Scope — `TickLoop.run()` existiert nicht). Legt die `DeviceProtocolPort`-Surface (`GG-AR-PORT-DRN-007`) fuer die fuenf M4-Sub-Adapter MQTT/Modbus/OPC-UA/DNP3/IEC fest. Vier Decisions aus [`done/M4-welle-0.md`](../planning/done-archive/M4-welle-0.md) §3 Decision-Liste: §2.1 Decision 2 **final** sync-`Protocol` mit Adapter-internem Event-Loop-Thread + Queue fuer async-Stacks (Welle 4 OPC-UA via `asyncua` traegt die Konstruktion zuerst real); §2.2 Decision 3 **final** expliziter Caller-Scope-Lifecycle via `TickLoop.start_protocol_ports()`/`stop_protocol_ports()` (FIFO-Start, LIFO-Stop, idempotent, Best-Effort-Partial-Cleanup mit `__context__`-Chain; Caller wrappt seine Tick-Schleife in `try/finally`; Replay-Mode skippt; Adapter-Lifetime == Run-Lifetime, nicht Service-Lifetime; **kein** `TickLoop.run()`); §2.3 Decision 7 **final** stateless aus Replay-Sicht, Reconnect-State volatile, Snapshot-Schema bleibt v2 in M4, reversibel via ADR-0015-Schema-Bump-Pfad; §2.4 Decision 1 **provisorisch** DNP3 + IEC-61850 Verzicht-Default, finale Disposition in M4-Welle 5 informiert durch asyncua-Erfahrung aus Welle 4 (§6 Verzicht-Anhang-Slot bleibt in Welle 1 leer; Welle 5 fuellt ihn entweder mit Verzicht-Text oder referenziert eigenen Spike-ADR). Welle 1 schreibt **keine** konkreten Adapter-Module — konkrete Implementer (`adapters/driven/protocol_mqtt/` etc.) kommen ab Welle 2. Status-Uebergang: `Proposed → Provisional` (M4-Welle-1-C3 `5f03bbf` + Linter-Folge `82f947c` nach C2-Merge `d09adf3` mit `device_protocol.py` + TickLoop-Lifecycle-Methoden + 23 neue Unit-Tests + `make arch-check`/`make gates` cache-frei gruen ohne Override; 19/19 Contracts KEPT = 7 lint-imports + 12 `tools/arch_check.py`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027). |
| 0031 | [MQTT-Adapter-Profile (M4 Welle 2)](0031-mqtt-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-2-Erstwurf (`Proposed` 2026-05-30 `4e102b8`) -> `Provisional` mit C3 (2026-05-30) nach C2-Merge `f33bb4e` (feat: `protocol_mqtt/` 7-Dateien-Modul + 50 neue Unit-Tests + Mosquitto-Integration-Smoke + `pyproject.toml`/`uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override). Vier Decisions aus [`done/M4-welle-0.md`](../planning/done-archive/M4-welle-0.md) §3 Decision 4: §2.1 Decision 4a **final** Topic-Schema inline im `protocol_ports`-Scenario-YAML-Block (kein separater `mqtt_profiles`-Top-Level — Welle 2 spart die Indirektion ein; separate Section bleibt als Welle-6-Schaerfungspfad offen); §2.2 Decision 4b **final** Payload-Codec `canonical_json` (M2-Welle-0a-Stand, Trigger-014-Quelle: `done/014-generic-snapshot-format-codec.md`; Trigger 004 [`open/004`](../planning/open/004-canonical-encoder-alternative-adr.md) `canonical encoder`-Alternative bleibt mit Re-Eval-Pfad in Welle 6 — Aktivierungs-Bedingung: messbarer Perf-Druck am MQTT-Publish-Throughput im realen Compose-Smoke); §2.3 Decision 4c **final** QoS-Default `QoS 0` fuer Telemetry-Publishes (high-volume, Verlust-tolerant), `QoS 1` fuer Command-Publishes (acknowledged) und `QoS 1` fuer Subscribe-Pfade — pro `topics.<device_id>.qos_*` ueberschreibbar; §2.4 Decision 4d **final** Callback->Sync-Marshal via Per-Target `queue.Queue` (`dict[device_id, queue.Queue]`) mit Lazy-Init im paho-mqtt-`on_message`-Callback (laeuft im paho-Loop-Thread), `read(target)` macht `get_nowait()` (nicht-blockierend, `None` bei leer), `write(target, command)` ruft `client.publish()` direkt (thread-safe per paho-Doku); `on_message`-Exceptions werden geschluckt+geloggt (LogPort, falls vorhanden) und **nicht** an Loop-Thread propagiert. Welle 2 schreibt **keinen** OTel-Span-Wrap (Welle 6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done-archive/M4-welle-1.md) §7). Status-Pfad: `Proposed → Provisional` (M4-Welle-2-C2-Merge nach feat-Commit mit `protocol_mqtt/`-Modul + 4 Unit-Test-Module + Integration-Smoke gegen Mosquitto-Sibling + `compose.yml`/`pyproject.toml`/`Dockerfile`-Edits + `make arch-check`/`make gates` gruen ohne Override) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030). |
| 0032 | [Modbus-TCP-Adapter-Profile (M4 Welle 3)](0032-modbus-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-3-Erstwurf (`Proposed` 2026-05-30 `a86ac46`) -> `Provisional` mit C3 (2026-05-30) nach C2-Merge `d721982` (feat: `protocol_modbus/`-5-Modul-Paket + 95 neue Unit-Tests + in-process pymodbus-Server-Integration-Smoke + `pyproject.toml`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override). Sechs Decisions: Register-Schema inline, Datatype-Set `{int16, uint16, int32, uint32, float32}`, direkt-sync, Function-Code-Mapping FC03/FC10 Defaults mit FC04/FC06-Overrides, Slave-Unit-ID per Target, in-process pymodbus-Server-Smoke. Review-Folge 2026-05-31: Smoke-Abdeckung auf Default-E2E-Profil praezisiert; [`done/031`](../planning/done-archive/031-modbus-adapter-review-folge.md) hat FC06-Multi-Register-Guard, Read-/Write-Fehler-Taxonomie und bewusste Smoke-Abgrenzung umgesetzt. Trigger 006 [`open/006`](../planning/done-archive/006-mypy-strict-bytes.md) ist nach positiver Re-Eval aktivierungs-reif; Aktivierung bleibt separater Folge-Slice. Welle 3 schreibt **keinen** OTel-Span-Wrap (Welle 6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done-archive/M4-welle-1.md) §7). Status-Pfad: `Proposed → Provisional` (M4-Welle-3-C3-Merge nach C2 `d721982`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031). |
| 0033 | [OPC-UA-Adapter-Profile (M4 Welle 4)](0033-opcua-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-4-Erstwurf (`Proposed` 2026-05-31 `74ed35b`) -> `Provisional` mit C3 `7ad5baf` (2026-05-31) nach C2-Merge `78fdd7a` (feat: `protocol_opcua/`-6-Modul-Paket + 81 neue Unit-Tests + 8 in-process `asyncua.Server`-Integration-Smokes parametrisiert ueber alle 8 Datatypes + `pyproject.toml`-Pin auf `asyncua>=1.2b2,<2.0` (Beta wegen Python-3.14-Forward-Reference-Inkompat in 1.1.8) + mypy-Override `implicit_reexport=true` fuer `asyncua.*` + `uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Coverage 95.16 %, Branch-Critical-Coverage 90.95 %). **Erster rein-async-Stack** im Repo — ADR 0030 §2.1-Konsequenz produktiv vorgetragen via eigener `OpcuaLoopThread`-Klasse (dedizierter `asyncio.AbstractEventLoop` in `threading.Thread(daemon=True)` + `run_coroutine_threadsafe`-Marshal; normativer Teardown-Vertrag mit Lifecycle-Lock und Start-Timeout). Fuenf Decisions alle **final**: O-a Node-ID-Schema inline (numerisch + String, Pattern aus ADR 0031/0032); O-b Async-Bridge via Loop-Thread (ADR-0011-Schaerfungspfad fuer Welle-6 Schwester-Port-ADR offen); O-c Datatype-Set `{Boolean, Int16, UInt16, Int32, UInt32, Float, Double, String}` (8 Werte; weitere Welle-6-Schaerfung); O-d Polling-Read + Direct-Write (Subscription-Pfad als Welle-6-Forward-Pointer); O-e in-process `asyncua.Server` (LGPL-3.0; Pattern-Praezedenz Welle-3-M-f). **Slice-032-Schaerfung** 2026-05-31 ([`done/032`](../planning/done-archive/032-opcua-adapter-review-folge.md)): 6 HIGH + 11 MEDIUM Code-Review-Findings umgesetzt — `threading.Lock` im `OpcuaLoopThread`, `start(timeout_s=)` + typed `OpcuaLoopThreadStartTimeoutError`, Port-Exception-Filter um `RuntimeError`/`CancelledError`, `Quality.INVALID`-Markierung fuer String-Reads, `Float`-32bit-Quantisierung im Codec, `OverflowError`-Catch, Pin-Range, Smoke-Server-`asyncio.Event`-Stop-Signal. ADR-Body geschaerft an §2.1 (Optional-Felder entfernt; YAGNI) und §2.5 (Test-Server-Loop bewusst getrennt von produktivem `OpcuaLoopThread`). Welle 4 schreibt **keinen** OTel-Span-Wrap (Welle 6), **keine** OPC-UA-Security (Anonymous-Endpoint; Welle 6 oder M6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done-archive/M4-welle-1.md) §7). Folge-Pflicht: asyncua-Pin auf `>=1.2,<2.0` ziehen, sobald 1.2 final auf PyPI ist (Welle-6- oder M6-Material). Status-Pfad: `Proposed → Provisional` (M4-Welle-4-C3-Merge nach C2 `78fdd7a`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032). |
| 0034 | [DNP3-Adapter-Profile (M4 Welle 5a)](0034-dnp3-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-5a-Erstwurf (`Proposed` 2026-05-31 `b0fea7e`) -> `Provisional` mit C3 (2026-05-31) nach C2-Merge `224b370` (feat: `protocol_dnp3/`-5-Modul-Paket + 56 neue Unit-Tests inkl. hypothesis-Codec-Property-Tests + 4 in-process `dnp3-outstation.AsyncOutstation`-Integration-Smokes parametrisiert ueber alle 4 Group/Variation-Kombinationen + `pyproject.toml`-Pin `nfm-dnp3>=1.0,<2.0` produktiv + `dnp3-outstation>=0.2,<1.0` als dev-only Test-Sibling + mypy-Overrides `ignore_missing_imports=true` fuer `dnp3py.*`/`dnp3_outstation.*` + `uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Tests 1462 Unit + 35 Integration). **Zwei-Library-Setup** (architektonisch neu in M4): Master/Client via `nfm-dnp3` 1.0.1 (PyPI, MIT, Pure-Python, sync API mit Thread-Lock-Schutz — C1-Probe-Run verifiziert) als Produktiv-Dependency; Outstation/Server via `dnp3-outstation` 0.2.0 (PyPI, MIT, Pure-Python, asyncio-native) **nur als Test-Sibling** in `[dependency-groups.dev]`. Welle 5a loest die in ADR 0030 §2.4 als „provisorisch" markierte DNP3-Verzicht-Disposition per Spike-Lieferung auf (Pattern ADR 0011; M4-Welle-7-Closure schaerft ADR 0030 §2.4 entsprechend). Fuenf Decisions alle **final**: §2.1 Decision D-a Point-Schema inline im `protocol_ports`-Block (Pattern-Praezedenz ADR 0031/0032/0033); §2.2 Decision D-b direkt-sync (Pattern-Praezedenz Welle-3-M-c — nfm-dnp3 ist sync, kein Loop-Thread noetig); §2.3 Decision D-c Group/Variation-Set `{Group 1/V1, Group 1/V2, Group 30/V1, Group 30/V5}` (Welle-5a-Minimum: Binary-Inputs + 32-bit Int/Float Analog-Inputs; Counter/Output/Event-Classes Welle-6-Schaerfung); §2.4 Decision D-d Class-0-Polling-Read mit Resultat-Filter-by-Index (DNP3-spec-konformes Idiom; `read_analog_inputs(start, stop)` verworfen wegen Wire-Compat-Limitation `dnp3-outstation` v0.2.0 supportet nur qualifier 0x00/0x06, nicht 0x01); §2.5 Decision D-e in-process `dnp3_outstation.AsyncOutstation` als Test-Sibling (Pattern-Praezedenz Welle-3-M-f + Welle-4-O-e). **C2-Library-Bug-Find**: `nfm-dnp3.AnalogInput.__repr__` zeigt `idx=0`, das tatsaechliche Attribut heisst aber `.index` — `_port._find_point` benutzt `getattr(point, "index", None)` ohne Wartepause. Welle 5a schreibt **keinen** DNP3-Write-Pfad (Welle 5b+, throws `Dnp3PortWriteNotImplementedError`), **kein** Event-Class-Polling (Welle 6), **keinen** OTel-Span-Wrap (Welle 6), **keine** DNP3-Security (Welle 6 oder M6) und **keinen** Planted-Violator-Property-Test (Welle 6). Status-Pfad: `Proposed → Provisional` (M4-Welle-5a-C3-Merge nach C2 `224b370`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032 + 0033). |
| 0035 | [IEC-61850-Adapter-Profile (M4 Welle 5b)](0035-iec61850-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-5b-Erstwurf (`Proposed` 2026-06-01 `88c1a33`) + C1-Review-Folge `da8aed9` (API-Korrektur read_value/write_value/Exception-Famille + Lizenz-Refit Optional-Extra statt [project] dependencies + M4-protocol-adapters.md-Sync nach 4 Findings) -> `Provisional` mit C3 (2026-06-01) nach C2-Merge `944bca5` (feat: `protocol_iec61850/`-5-Modul-Paket inkl. ImportError-Guard fuer Optional-Extra + 75 neue Unit-Tests inkl. hypothesis-Codec-Properties + Integration-Smoke unter **2c-Mock-only-Fallback aktiv** + NEU `LICENSES/GPL-3.0.txt` + LICENSE-Hinweis-Block + README+README.de-Lizenz-Sektion + SPDX-Header in 11 Files + NEU `tests/integration/fixtures/iec61850/simpleIO.cfg` libiec61850-natives CFG-Modell + `pyproject.toml`-Pin `pyiec61850-ng>=1.6,<2.0` in `[project.optional-dependencies.iec61850]` + mypy-Override `module="pyiec61850.*"` + `Dockerfile`-Edit `uv sync --extra iec61850` in deps/source-Stages + `CRITICAL_COV_TARGETS` um `adapters/driven/protocol_iec61850` + `compose.yml`-Header-Kommentar-Sync; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Tests 1537 Unit + 35 Integration passed + 4 IEC-Smokes skipped via `pytest.mark.skip` mit 2c-Fallback-Begruendung). **Erstmaliger Repo-Praezedenzfall** fuer GPL-isolierte Sub-Module in einem sonst MIT-Projekt (Decision I-f). Welle 5b loest den letzten ADR-0030-§2.4-Verzicht-Default (IEC-61850) per Spike-Lieferung auf (Pattern ADR 0011; M4-Welle-7-Closure schaerft ADR 0030 §2.4 auf „durch Welle-5a (DNP3) **und** Welle-5b (IEC-61850) aufgeloest"). Sechs Decisions alle **final**: §2.1 Decision I-a LN/CDC-Schema inline; §2.2 Decision I-b direkt-sync (Pattern-Praezedenz Welle-3-M-c + Welle-5a-D-b); §2.3 Decision I-c MMS-Datatype-Set `{bool, int32, float, string}` + FC-Mapping `{MX, ST, SP, CF, DC}` mit Adapter-Default `MX`; §2.4 Decision I-d Per-Target `MMSClient.read_value(reference, fc)`; §2.5 Decision I-e **2c-Mock-only-Fallback aktiviert** (Probe-Run auf Python 3.12 lief Float/Int32/String-Roundtrip mit `IedServer(model_path=fixture)` sauber durch, aber Docker-Stack Python 3.14 segfaultet im `_pyiec61850.so` SWIG-Layer mit exit 139 beim ersten `IedServer.start()` — pyiec61850-ng 1.6.1.2 manylinux1_x86_64-Wheel-ABI-Inkompat unter 3.14; Welle-6-Schaerfungspfade dokumentiert: Python-3.12-Runtime fixieren ODER Library-Upgrade ODER Wheel-Rebuild gegen 3.14); §2.6 **NEU Decision I-f Lizenz-Boundary** GPLv3-Isolation auf `protocol_iec61850/*` + zugehoerige Tests via SPDX-Header pro Datei + Optional-Extra-Setup; Rest grid-gym bleibt MIT. Welle 5b schreibt **keinen** Write-Pfad (Welle 6+, wirft `Iec61850PortWriteNotImplementedError`), **kein** RCB-Subscription (Welle 6), **kein** GOOSE-Publishing/Subscription (Welle 6), **kein** Sampled-Values (Welle 6 oder M6), **keinen** OTel-Span-Wrap (Welle 6), **keine** IEC-61850-Security (Welle 6 oder M6), **keinen** Planted-Violator-Property-Test (Welle 6) und **keine** aarch64-Wheel-Unterstuetzung (Welle 6 falls Bedarf). Status-Pfad: `Proposed → Provisional` (M4-Welle-5b-C3-Merge nach C2 `944bca5`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032 + 0033 + 0034). **Slice 033** (M4-Welle-5b-C2-Review-Folge 2026-06-01, [`done/033`](../planning/done-archive/033-iec61850-adapter-review-folge.md)): 15 Findings adressiert (10 HIGH + 5 MEDIUM) — Sentinel-Exception-Klasse statt `Exception`-Alias im Optional-Extra-Off-Pfad, `_PyIecMMSError`-Catch-All-Basis in `start()`, `stop()` State-Mutation NACH `disconnect()`, NaN/Inf-Reject + int-Reject in `_decode_float`, Container-Check gated auf non-string, NEU `Iec61850PortReadConnectionLostError` (mid-flight-NotConnected), Config-Anti-Scope-write-Reject bei Konstruktion, `TelemetryPoint.value`-Decimal-Wrap mit `Quality.INVALID`-String-Fallback (Welle-4-Slice-032-Pattern), Sub-Millisekunden-Timeout-Floor, `Dockerfile`-`build-app`-Stage `--extra iec61850`-Propagation, `simpleIO.cfg`-SPDX-Header + Derivative-Work-Attribution, `pyproject.toml`-GPL-Classifier ergaenzt. ADR-Status bleibt `Provisional`. |
| 0036 | [UI-Stack-Wahl (M5)](0036-ui-stack-choice.md) | Provisional | 2026-06-01  | Pre-M5-Welle-0-Sondierungs-ADR (analog ADR 0030 fuer M4-Welle-1-Pattern). Erstwurf (`Proposed`) 2026-06-01 mit `f4a9ced` + `e0c3f66` (Charting-Sub-Decision §2.5). **Provisional 2026-06-01** mit M5-Welle-1-C1 nach Pre-C0c-HTMX-FastAPI-Smoke-Probe-Run `9c20dad` (4 Probe-Tests in `tests/integration/test_m5_welle_1_htmx_probe.py` validieren server-side die drei kritischen Composition-Punkte: FastAPI HTML-Response, HTMX `HX-Request`-Pattern, WebSocket Server-Push). **Option 1 fixiert** (FastAPI + HTMX + Jinja2 + Chart.js; Single-Stack-Python, 10 A-1-Gates, ~6 Wellen). Optionen 1b (SvelteKit-SPA), 2 (React-SPA) und 3 (Streamlit ausgeschlossen) bleiben als dokumentierte Alternativen fuer Welle-6+/M6-Migration. Status-Pfad: `Proposed` → `Provisional` (dieser Commit) → `Accepted` (M5-Welle-7-Closure, analog ADR 0030..0035). |
| 0037 | [HTTP-API-Surface-Pattern (M5 Welle 1)](0037-http-api-surface-pattern.md) | Accepted    | 2026-06-01  | M5-Welle-1-C1 `d468e68` (`Proposed`, zusammen mit ADR-0036-Schaerfung) → **`Provisional` mit M5-Welle-1-C3 `f9f514d` nach C2-Code-Merge `ae630ce`** (5 REST + 1 WebSocket-Endpunkte + Pydantic-Schemas in 4 neuen Modulen unter `src/grid_gym/adapters/driving/http_api/`; 16 neue Unit-Tests + 2 Integration-Tests; 1600 unit + 41 integration Tests gruen; 10/10 A-1-Gates ohne Override). Schaerft die HTTP-API-Surface (`GG-API-001..004` aus Lastenheft §16) konkret fuer Welle-1-Implementation und schliesst Decisions 4 + 9 + Roadmap-Typo (siehe `../planning/done/M5-welle-0.md §3`). **Decision API-1** (Replay-Controls-Vertrag) — `POST /runs/{id}/control` mit Action-Body (Variante B; kompakte Surface, erweiterungs-fest); produktiv in `_runs_action_router.py:post_run_control` mit `ControlRequest.action: Literal["pause","resume","stop"]`-Body-Validation. **Decision API-2** (UICommandPort-Separation) — **kein separater Slot**; UI nutzt HTTP-API direkt via REST + WebSocket (YAGNI; konsistent mit M4-Pattern eines Driving-Port-Slots pro Architektur-Funktion); produktiv durch Abwesenheit eines `UICommandPort`-Slots im Repo. **Decision API-3** (Roadmap-Typo) — `GG-AR-PORT-DRG-002`-Slot verworfen (Typo gegen `DRV-*`-Konvention; semantisch ueberfluessig nach Decision API-2); produktiv in M5-Welle-1-C3 `f9f514d` durch `roadmap.md §3 M5`-Edit. Status-Pfad: `Proposed` → `Provisional` (M5-Welle-1-C3 `f9f514d`) → `Accepted` (M5-Welle-7-Closure analog ADR 0030..0036). **Schaerfungen**: Pydantic-Schema-Substanz fuer Request-Bodies geschaerft durch [`ADR 0045`](0045-http-api-request-strict-validation.md) (Schaerfung-ohne-Supersedes per ADR 0011; M6-Welle-5b-C1): Strict-Mode + `extra="forbid"` als Pflicht-Vertrag fuer alle REST-Request-Bodies via NEU `_BaseRequest`-Mixin in `_schemas.py`; Response-Bodies + WebSocket-Inputs bleiben unveraendert. ADR 0037 §2.1 + §2.2 + §2.3 bleiben textlich unangetastet. |
| 0038 | [TelemetryStreamPort (M5 Welle 3)](0038-telemetry-stream-port.md) | Provisional | 2026-06-01  | M5-Welle-3-C1 `9f3c00d` (`Proposed`) → **`Provisional` mit M5-Welle-3-C3 (dieser Commit) nach C2-Code-Merge `82bdf39`** (NEU `TelemetryStreamPort` unter `hexagon/ports/driving/` + NEU `InMemoryTelemetryStream` + `DemoTelemetryGenerator` unter `adapters/driven/telemetry_stream_inmemory/` + WS-Subscribe-Wiring + Dashboard-UI-Page mit Chart.js-Time-Series + 6 Quality-Marker-CSS-Klassen; 16 neue Unit-Tests + 2 Integration-Tests; 10/10 A-1-Gates ohne Override). Schliesst M5-Welle-0-Decision 11 (Telemetry-Source-Architektur). **Decision 11a** (Surface-Form) — `subscribe(run_id) -> AsyncIterator[TelemetryPoint]` als Async-Generator (PEP 525); Alternativen Callback-Register + Queue-Pull + Subject/Observable verworfen. `TelemetryPoint`-Dataclass mit Feldern `run_id`/`device_id`/`metric`/`value`/`unit`/`simulation_time_ms`/`quality (Literal 6 Zustaende)`/`sequence` deckt `GG-API-002` + `GG-UI-002` + `GG-UI-009`-Akzeptanz ab; produktiv in `stream.py`. **Decision 11b** (Backpressure) — Drop-Oldest mit bounded `asyncio.Queue` pro Subscriber (Default `maxsize=128`, ~1.3s Buffer bei 100ms-Tick); juengste Daten ueberleben; Probe-Run-Beleg `5349923`; produktiv in `publish` via `contextlib.suppress(asyncio.QueueEmpty)`. **Decision 11c** (Lifecycle) — `try/finally`-Cleanup im AsyncIterator-Body garantiert deterministische Subscriber-Slot-Freigabe bei `aclose()` / WebSocketDisconnect; produktiv im `subscribe`-Generator. Out-of-Scope: TickLoop-Wiring (Welle 4), Persistence-Sink, OTel-Span-Wrap (M6), SSE-Fallback (M6). Status-Pfad: `Proposed` → `Provisional` (dieser Commit) → `Accepted` (M5-Welle-7-Closure, analog ADR 0030..0037). |
| 0039 | [Run-Control + RunStatus-Tracking (M5 Welle 4a)](0039-run-control-and-status-tracking.md) | Provisional | 2026-06-02  | M5-Welle-4a-C1 `f1284c4` (`Proposed`) → **`Provisional` mit M5-Welle-4a-C3 (dieser Commit) nach C2-Code-Merge `9c188e0`** (NEU `RunStatus`-Literal-Alias + RunRepository-Extension `update_status`/`get_status` + TickLoop-Control-Surface mit konsolidierter `request(action)`-Methode + NEU `TickLoopRegistry`-Adapter + NEU `DemoTickLoopDriver` + NEU UI-Page `GET /control` mit HTMX-Polling + NEU `_demo_setup.py`-Komposition-Root; +24 Unit + 1 Integration-Test = 1650 unit + 50 integration; 10/10 A-1-Gates ohne Override). Verankert die Replay-Controls-Architektur fuer die `POST /runs/{id}/control`-Wiring-Welle und schliesst drei NEUE Decisions (12/13/14) aus dem Welle-4a-Slice-Doc. Definiert eine **Driven-Port-Extension** (`RunRepositoryPort.update_status` + `get_status`) plus eine **TickLoop-interne Cooperative-State-Machine** (`_control_state` + `request(action)` + Pre-Tick-Guard) plus ein **HTMX-Polling-Pattern** fuer den UI-Status-Block. **Decision 12** (RunStatus-Tracking-Architektur) — RunRepository-Extension; NEU `RunStatus`-Literal-Alias mit 5 Welle-1-aligned-Werten (`pending`/`running`/`paused`/`stopped`/`completed`; C2-Realization-Anpassung statt Slice-Doc-Original `idle`/`ended` wegen Welle-1-`RunState`-Vokabel-Konsistenz); Frozen-`RunMetadata` bleibt unangetastet; Alternativen `RunMetadata.status` mutable (bricht Frozen-Equality fuer Reproduzierbarkeit; ADR 0007) + separater `RunStatusPort` (YAGNI) + nur TickLoop-internes Feld (bricht Restart-Persistenz) verworfen; produktiv in `domain/run.py` + `_fakes.py:InMemoryRunRepository` (`_status_store`-Dict + `save`-Auto-`pending`). **Decision 13** (TickLoop-Control-Surface) — Cooperative state-machine mit konsolidierter `request(action: ControlAction)`-Methode (C2-Realization-Anpassung statt der Slice-Doc-Original-3-`request_pause`/`request_resume`/`request_stop`-Wrapper wegen `PLR0904 max-public-methods=12`; Transition-Matrix semantisch identisch ueber Modul-Konstante `_CONTROL_ACTION_TRANSITIONS`); externer asyncio-Tick-Driver im FastAPI-Lifespan; Pattern-Praezedenz ADR 0022 §2.4 (FaultPort-TickLoop-Hook im Vor-Tick-Block); NEU `TickResult.paused_result`-Classmethod-Factory + NEU Errors `TickLoopStoppedError` + `TickLoopInvalidTransitionError`; Snapshot-Format (ADR 0015) bleibt unveraendert (`_control_state` ist Run-Lifecycle, nicht Tick-Determinismus); Alternativen internes Threading (bricht `GG-SIM-001`) + separater `ControlPort` (YAGNI) verworfen — die Verzicht-auf-Port-Begruendung ist in `pyproject.toml`-`AC-ADAPTER-PURE`-`ignore_imports`-Block verankert (Komposition-Root-Brueck-Erlaubnis fuer `_demo_setup.py` + `_tick_loop_registry.py` + `_tick_loop_driver.py` → `hexagon.core.simulation.{tick_loop,scheduler}`). **Decision 14** (Replay-Status-Update-Pattern) — HTMX-Polling auf `GET /runs/{id}/status` mit 1s-Trigger (Welle-1-Stub ausgewirt statt NEU-Endpoint) + `RunStatusResponse`-JSON; WS-Surface aus ADR 0038 bleibt fuer High-Frequency-Telemetry reserviert; Alternativen zweite WS-Surface (overkill) + Embedded in TelemetryPoint (bricht ADR-0038-Semantik) verworfen; produktiv in `_runs_router.py:get_run_status` + UI-`_control_content.html`-Inline-JS. Vorbild-Probes Welle-1 `9c20dad` (HTMX-FastAPI) + Welle-3 `5349923` (Asyncio-Pub/Sub) decken Mechanik bereits ab — kein eigener Welle-4a-Probe noetig. Out-of-Scope: Alarm-Aggregation (Welle 4b), Scenario-Loader (Welle 5), Postgres-Status-Persistenz (M3-Welle-6c; `PostgresRunRepository.update_status`/`get_status` werfen `NotImplementedError` mit Forward-Pointer), Snapshot-Status-Roundtrip, OTel-Span-Wrap (M6), WS-Status-Push (M6), Start-Action (idempotenter Resume aus `pending`), Resume aus `stopped` (neue `run_id` ist Welle-5-Scenario-Concern). Status-Pfad: `Proposed` → `Provisional` (dieser Commit) → `Accepted` (M5-Welle-7-Closure, analog ADR 0030..0038). |
| 0040 | [Alarm-Aggregation + AlarmStreamPort (M5 Welle 4b)](0040-alarm-aggregation-and-stream-port.md) | Provisional | 2026-06-02  | M5-Welle-4b-C1 `850cf85` (`Proposed`) → **`Provisional` mit M5-Welle-4b-C3 (dieser Commit) nach C2-Code-Merge `b7ac7b3`** (+31 Unit + 1 Integration-Test = 1681 unit + 51 integration; 10/10 A-1-Gates ohne Override). C2-Realization-Notes (4 Anpassungen ggu. Slice-Doc/C1-ADR-Original, alle in C3 verankert): (1) REST-Pfad `/alarms-history` statt `/alarms` (FastAPI-Routenkonflikt mit UI-Page; UI behaelt Navigations-Convention); (2) Mapper-Familie in `core/simulation/alarm_mappers.py` statt `core/domain/alarm.py` ([`AC-PORTS-NO-OUT`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert): `ports` darf nicht transitiv auf `core/devices` via `Alarm`-Type zugreifen); (3) Power-Mapper-Konsolidierung 4→1 zu `alarm_from_power_device_alarm(PowerDeviceAlarm = BatteryAlarm | PvAlarm | LoadAlarm | GridConnectionAlarm, ...)` ([`AC-NO-GOD-UTILS`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert) max=5; SmartMeter-Mapper separat wegen abweichendem 4-Feld-Schema; `dispatch_alarm_mapper` als Routing-Entry-Point = 3 public functions); (4) `_alarm_setup.py`-Auslagerung aus `app.py` wegen [`AC-NO-GOD-UTILS`](0002-language-and-build-stack.md#a-1--architekturtests-verbindlich-automatisiert) (Pattern analog Welle-4a-`_demo_setup.py`). Verankert die Alarm-Architektur fuer die Welle-4b-Lieferung und schliesst drei NEUE Decisions (15/16/17) aus dem Welle-4b-Slice-Doc. Loest die Welle-2-Review-M-3-Forward-Pointer-Erbschaft aus [ADR 0014 §6](0014-battery-snapshot-schema.md) („AlarmSinkPort kommt mit M3"; M3 ohne Sink geschlossen) produktiv aus — Welle 4b liefert die **Driving-Side-Variante** (`AlarmStreamPort` + adapter-interner `AlarmHistoryBuffer`); Postgres-Persistenz (`GG-PERSIST-004`) bleibt M3-Welle-6c-Material (NEU `PostgresAlarmRepository` + NEU `AlarmRepositoryPort`-Driven-Slot, siehe §3.3). Definiert eine **NEU Domain-Type-Familie** (Unified `Alarm` mit kanonischem 9-Feld-Schema per [`spec/architecture.md §Alarm`](../../../spec/architecture.md) + `AlarmSeverity`/`AlarmStatus`-Literals + 5 typisierte Mapper-Funktionen) plus eine **TickLoop-interne Aggregations-Sequenz** (`TickResult.emitted_alarms`-Feld + Drain-Hook am Tick-Ende) plus eine **NEU Driving-Port-Surface** (`AlarmStreamPort` 1:1 parallel zu `TelemetryStreamPort` aus ADR 0038) plus REST-History-Endpoint + WS-Live-Endpoint mit adapter-internem Ring-Buffer. **Decision 15** (NEU Unified `Alarm`-Domain-Schema + Mapper-Familie) — Domain-Slot mit 9-Feld-Frozen-Dataclass (`alarm_id`/`run_id`/`simulation_time_ms`/`target`/`code`/`severity`/`message`/`status`/`fault_id?`) + `AlarmSeverity = Literal["info","warning","critical"]` + `AlarmStatus = Literal["active"]` (Welle-4b nur `active`; Lifecycle-Erweiterung Welle 6+/M6-Anti-Scope) + 5 reine Mapper-Funktionen (`alarm_from_battery_alarm`/`alarm_from_pv_alarm`/`alarm_from_load_alarm`/`alarm_from_grid_connection_alarm`/`alarm_from_smart_meter_alarm`) mit Mapping-Heuristik `(result, limit, limit_unit) → (code, severity, message)` per `PvAlarm`-Docstring-Pattern (LIMITED → `power_clamp_limited`/`warning`; REJECTED → `command_rejected`/`critical`; SmartMeter-REJECTED → `smart_meter_rejected`/`warning`); Device-Alarm-Klassen bleiben unveraendert (~41 Tests passing); Alternativen A Device-Klassen erweitern (bricht Hexagonal-Reinheit + hoher Test-Migration-Aufwand) + C Adapter-Side-Schema (bricht Domain-Adapter-Separation) verworfen. **Decision 16** (TickLoop-Alarm-Aggregation via `TickResult.emitted_alarms`) — Aggregations-Slot mit NEU `emitted_alarms: tuple[Alarm, ...] = ()`-Feld in `TickResult` (Backward-Compat via Default analog Welle-4a-`paused: bool = False`-Pattern) + NEU `alarm_id_source: Callable[[], str] | None = None`-TickLoop-Konstruktor-Kwarg (UUIDv4-Source-Injection fuer testbare Deterministik analog `random: RandomPort` aus M1) + Drain+Map+Aggregate-Hook am Ende des `_run_tick_body` in Device-Konstruktor-Reihenfolge; Snapshot-Format (ADR 0015) bleibt unveraendert (Tick-Output, nicht persistenter State); Alternativen B separater `AlarmAggregator` (YAGNI) + C Adapter-Side-Drain (bricht Determinismus) verworfen. **Decision 17** (AlarmStreamPort-Surface + REST-History + WS-Live) — Adapter-Slot mit NEU `AlarmStreamPort`-Driving-Port-Surface 1:1 parallel zu `TelemetryStreamPort` (ADR 0038 §2.1; AsyncIterator-Pattern + `publish`/`subscribe`/`subscriber_count`) + NEU `InMemoryAlarmStream`-Adapter mit Drop-Oldest-Backpressure + bounded `asyncio.Queue(maxsize=64)` (kleiner als Telemetry's 128 weil Alarms niederfrequent) + NEU `AlarmHistoryBuffer`-Adapter-Helper (Ring-Buffer N=200; kein neuer Port — adapter-internes Helper, das M3-Welle-6c durch `PostgresAlarmRepository` ersetzt) + NEU REST-Endpoint `GET /runs/{run_id}/alarms` (Hydration; Query-Param `?limit=50`) + NEU WS-Endpoint `WS /runs/{run_id}/alarms-stream` (Live-Stream; Path-separat von REST-`/alarms` fuer Klarheit); UI nutzt beide via HTMX-`hx-get` (Initial-Render) + `hx-ext="ws" ws-connect=...` (Live-Updates) — Best-of-both gegen Tab-Reload-Resilience; 6-Spalten-Tabelle per `GG-UI-005`-Akzeptanz (Zeit/Ziel/Schweregrad/Code/Nachricht/Status) + 3 `severity-*`-CSS-Klassen analog Welle-3-Quality-Marker; Alternativen A nur WS (verliert History bei Tab-Reload) + B nur GET-Polling (verschwenderisch + Update-Latenz) verworfen. Vorbild-Probes Welle-3 `5349923` (Asyncio-Pub/Sub) + Welle-4a `f1284c4`+`9c188e0` (HTMX-Polling + REST-Hydration) decken Mechanik bereits ab — kein eigener Welle-4b-Probe noetig. Out-of-Scope: Status-Lifecycle (`acknowledged`/`resolved`; Welle 6+/M6), Postgres-Alarm-Persistenz (M3-Welle-6c), Device-Alarm-Klassen-Migration (5 Klassen unveraendert), `AlarmSinkPort`-Driven-Slot (Welle-6c-Material), Fault-ID-Mapping (Welle 6+), Scenario-Loader / Multi-Run-Multiplexing (Welle 5), OTel-Span-Wrap (M6), Cooperative-Pause-on-Critical-Alarm (Welle 6+; in ADR 0039 §3.2 als Symmetrie-Forward-Pointer angekuendigt). Status-Pfad: `Proposed` → `Provisional` (Welle-4b-C3 nach C2-Code-Merge) → `Accepted` (M5-Welle-7-Closure, analog ADR 0030..0039). |
| 0041 | [Performance-Bench-Pattern + Regression-Schwelle (M6 Welle 4b-a)](0041-performance-bench-pattern.md) | Accepted    | 2026-06-06  | M6-Welle-4b-a-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Loest M6-Welle-0-D-4-Vorbelegung + M6-D-7-Bench-Framework-Vorbelegung auf. Verankert `make perf` als ADR-0041-konformen NICHT-A-1-Quality-Gate-Pfad: **§2.1** Bench-Framework `pytest-benchmark>=4.0,<6.0` (Aufloesung M6-D-7; Konsistenz mit pytest-Infrastruktur). **§2.2** Mess-Protokoll: `median`-Statistik, `--benchmark-min-rounds=10`, `--benchmark-disable-gc`, `--benchmark-warmup=on`; Akzeptanz-Asserts pro Bench-Test. **§2.3** Regression-Schwelle 20 % Median-Drift gegen `tests/perf/baseline.json` (pytest-benchmark `--benchmark-compare-fail=median:20%`); Baseline-Updates per `perf: baseline update — <reason>`-Commit-Pattern. **§2.4** Bench-Locations `tests/perf/` (eigene Top-Level-Test-Tree-Branche; kein Coverage-Beitrag). **§2.5** Run-Form: separater Dockerfile-`perf`-Stage + `make perf`-Target; **NICHT** in `make gates`/`make ci`/`make fullbuild` (Bench-Lauf zu teuer fuer A-1-Aggregat). **§2.6** Baseline-Pinning: committed JSON-Snapshot `tests/perf/baseline.json` (Maintainer-Dev-Host-Mess; Cross-Maschinen-Vergleich nicht garantiert). Sub-Welle-4b-b/4b-c koennen ADR-0041-§2.2/§2.3 per ADR-0011-Schaerfung erweitern, ohne den Welle-4b-a-Vertrag zu brechen. Erst-Anwendungsfall: `GG-RT-004` SOLLTE (100 Geraete × 10 000 Ticks ohne verlorene Events) in Welle-4b-a-C2. Out-of-Scope: Framework-Wechsel (pyperf/asv waeren ADR-Schaerfung), CI-Pflicht-Gate `perf.yml` (Welle-4b-Closure-Material), Continuous-Performance-Tracking, Cross-Maschinen-Bench-Vergleich, `tests/perf/`-Coverage-Pflicht, Backpressure-Healthcheck-Surface (`GG-RT-001`; Welle-4b-c), Snapshot-Envelope-v2-Body-Serialisierung (M5-Erbschaft), Per-Geraete-Mikro-Bench. Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0042 + ADR 0043 + ADR 0044; Pattern analog M5-Welle-7-C1 `62f988d`). |
| 0042 | [SBOM-Tool + Release-Workflow-Pattern (M6 Welle 2)](0042-sbom-tool-and-release-pattern.md) | Accepted    | 2026-06-05  | M6-Welle-2-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung zusammen mit Trigger-008-Hash-Anchor-Block: M5-Welle-0-C2 `112efd3` = Trigger-Eroeffnungs-Stand, M6-Welle-0-C2 `74d9452` = `Active in M6-Welle-2`-Markierung, M6-Welle-2-C2 = `235395e` (Aufloesungs-Hash; NEU release.yml + Makefile/Dockerfile-Pflichtscope, siehe ADR §5)). Verankert `make sbom` + Release-Workflow als kanonisch dokumentierte Pflicht-Gate-Vertraege fuer `GG-CICD-007` SOLLTE-Akzeptanz (Schwester-Pattern zu ADR 0029 + ADR 0043; alle drei sind Quality-Gate-Vertraege parallel zu ADR-0002-§A-1, ohne ADR 0002 textlich zu beruehren). **§2.1** SBOM-Tool + Scan-Ziel: `anchore/syft:v1.17.0` + `cyclonedx-json` gegen `grid-gym-runtime:latest` (Runtime-Image, NICHT Source-Tree; `sbom: build`-Dependency analog `image-audit: build` aus `Makefile` Z.279) — Tool-/Format-/Scan-Ziel-Wechsel ADR-pflichtig per ADR-0011-Pattern; Pre-C0c-Probe-Beleg im M6-Welle-2-Slice-Doc §1.2 belegt funktionale Aequivalenz zu Trivy (164 vs. 169 Komponenten, beide CycloneDX v1.6; Syft bleibt Status quo wegen PEP-503-Naming-Konvention + bereits im Makefile). **§2.2** Release-Workflow-Trigger: Hybrid Tag-Push (`v*.*.*`) + `workflow_dispatch`-Fallback mit `concurrency`-Block gegen parallele Laeufe; VERSION-Variable aus `github.ref_name` (Tag) oder explizitem Input (Manual). **§2.3** Asset-Bundling: 6 publizierte Artefakte pro Release = 1× GHCR-Push (Container-Image) + 5× GitHub-Release-Asset-Files (SBOM-CycloneDX-JSON + Test-Reports-JUnit-XML + Coverage-HTML-Tarball + OpenAPI-JSON + Demo-Abnahme-Markdown). Separate-Files-Pattern (kein Single-Archive) wegen einzeln-Pruefbarkeit der Lastenheft-Klassen; Coverage-HTML als Tarball wegen Verzeichnis-Struktur. **§2.4** Container-Registry: GHCR (`ghcr.io/<owner>/grid-gym`) mit `GITHUB_TOKEN` + `packages: write`-Workflow-Permission; kein externer Secret-Manager. Erst-Anwendungsfall: Trigger 008 (SBOM-Aktivierung; M5-Welle-0-Eroeffnung); M6-Welle-2-C2 loest auf, M6-Welle-2-C3 verankert Aufloesungs-Hash + Trigger-008-`open/ → done/`-Move (Pattern analog Trigger 010 in M6-Welle-1-C3). Out-of-Scope: SBOM-Tool-Wechsel (Syft-Default; Trivy aus ADR 0043 bleibt orthogonal), SPDX-Format, CI-Pflicht-Gate fuer `make sbom` (Welle-3-Material), Container-Image-Signing (M7+/Welle-5-Security), Release-Notes-Auto-Generierung, Multi-Architektur-Build (linux/amd64-only; ARM64 = M7+), alternative Registries (Docker Hub etc.; ADR-Schaerfung), PyPI-Wheel-Publishing, Branch-Release-Strategy. Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0041 + ADR 0043; Pattern analog M5-Welle-7-C1 `62f988d`). |
| 0043 | [Image-Audit-Strategie + Trivy-Defer-Aufloesungs-Pattern (M6 Welle 1)](0043-image-audit-strategy.md) | Accepted    | 2026-06-05  | M6-Welle-1-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung zusammen mit Trigger-010-Hash-Anchor-Block: M3-Welle-7 `c61ab0d` = Drift-Origin, M4-Welle-7 = Defer-Pfad-Erbschaft, M6-Welle-1-C2 = `b514170` (Aufloesungs-Hash; Null-Code-Edit-Welle, siehe ADR §5)). Verankert `make image-audit` (`GG-QG-002 SOLLTE`) als kanonisch dokumentierten Pflicht-Gate-Vertrag (Schwester-Pattern zu ADR 0029-`AC-NO-COVERAGE-PRAGMA`): **§2.1** Pflicht-Schwelle `TRIVY_SEVERITY=HIGH,CRITICAL` plus `--ignore-unfixed` (`Makefile` Z.25-26 + Z.279-294) als feste ADR-Substanz — Schwellen-Lockerung/-Verschaerfung ADR-pflichtig per ADR-0011-Pattern. **§2.2** Einzig zulaessige Defer-Form fuer HIGH/CRITICAL-CVEs mit Fix: `open/`-Trigger mit konkreter CVE-ID + Fix-Version + Aktivierungs-Pfad plus M-results-§4-S-4-Note; verboten als ADR-Bruch: `.trivyignore` ohne `open/`-Begleit-Trigger, `--skip-vuln`-Argument, `make image-audit`-Entfernung aus `make ci`/`make fullbuild`, stilles `--ignore-unfixed`-Toggle, `TRIVY_SEVERITY`-Lockerung ohne ADR-Schaerfungs-Commit. **§2.3** Defer-Aufloesungs-Pattern: `git mv open/ → done/` (rename-only) + `carveouts.md`-Sync + Hash-Anchor-Block im verankernden ADR + Top-Level-Doku-Sync. Erst-Anwendungsfall: Trigger 010 (krb5-CVE-Famille seit M3-Welle-7 `c61ab0d` als pre-existing `make fullbuild`-rot; fuehrende ID `CVE-2026-40356` in `libkrb5-3`/`libk5crypto3`/`libgssapi-krb5-2` mit Fix `1.21.3-5+deb13u1`); M6-Welle-1-C2 loest auf, M6-Welle-1-C3 verankert Aufloesungs-Hash + Trigger-010-`open/ → done/`-Move. Out-of-Scope: Scanner-Wahl (Trivy-Default `aquasec/trivy:0.58.0` aus `Makefile` Z.25; Wechsel waere M7+ Tooling-Slice), SBOM-Workflow (ADR 0042, Welle 2), CI-Pflicht-Gate fuer `make fullbuild` (Welle-1-D-1), Container-Hardening-Vollausbau (M6-Welle 6), per-CVE-Allowlist (ADR-Bruch per §2.2). Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0041 + ADR 0042; Pattern analog M5-Welle-7-C1 `62f988d`). **Schaerfungen**: §2.2-Defer-Form geschaerft durch [`ADR 0044`](0044-generated-trivyignore-permit.md) (Schaerfung-ohne-Supersedes per ADR 0011): zusaetzlich zur `open/`-Trigger-Pflicht ist eine generierte `.trivyignore` aus `deploy/security/vulnignore.yaml` mit Pflicht-`reason`/`expires`/`scope`-Feldern als Temp-Deferral-Form zulaessig. ADR 0043 §2.1 (Pflicht-Schwelle) + §2.3 (Defer-Aufloesungs-Pattern) bleiben unangetastet. |
| 0044 | [Generated-Trivyignore-Permit aus Audit-Source-of-Truth (M6 Welle 4a)](0044-generated-trivyignore-permit.md) | Accepted    | 2026-06-06  | M6-Welle-4a-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung zusammen mit Trigger-033-Hash-Anchor-Block: Welle-3-Post-Closure `ede21ad` = Trigger-Eroeffnungs-Stand; M6-Welle-4a-C2 = Pattern-Import-Hash, in §5 nachgetragen). Schaerft ADR-0043 §2.2-Defer-Form per ADR-0011-Pattern (additive Erweiterung, kein Supersedes): **§2.1** Permit-Form fuer generierte `.trivyignore` aus `deploy/security/vulnignore.yaml`-Audit-Source-of-Truth, ergaenzt ADR-0043 §2.2-`open/`-Trigger-Pfad um eine zweite Variante (`open/`-Trigger PLUS vulnignore.yaml-Eintrag als Temp-Deferral; `open/`-Trigger-Pflicht bleibt in Kraft). **§2.2** Schema-Pflicht-Felder: `id` + `reason` + `expires` (max. +90 Tage) + `scope`; Eintraege ohne diese Felder fuehren zu Render-Script-Bruch. **§2.3** Render-Script-Vertrag `tools/render_trivyignore.py`: liest YAML, filtert per Scope-Argument, bricht abgelaufene oder feldlose Eintraege, schreibt Plain-Text-`.trivyignore` (NICHT versioniert; `.gitignore`-Eintrag). **§2.4** Trivy-Integration via `--ignorefile`-Argument (Datei-basiert, NICHT `--skip-vuln`-CLI-Allowlist; ADR-0043 §2.2-Verbot bleibt). Pattern-Vorbild: m-trace-Schwester-Repo (`scripts/render-trivyignore.sh` + `.security/vulnignore.yaml`). Erst-Anwendungsfall: Trigger 033 (OTel-Collector Go-stdlib CVE-2026-42504 in `otel/opentelemetry-collector-contrib:0.153.0` gegen go1.26.3; Stable-Release mit go1.26.4+ erwartet 2026-06-09 bis 2026-06-12). Trigger 033 bleibt OFFEN als Stable-Watch (vulnignore-Pattern ist Temp-Deferral, echte Aufloesung weiter bei Stable-Release-Bump). Out-of-Scope: Aufhebung von ADR-0043 §2.2-Verboten (Bloss-`.trivyignore` + `--skip-vuln` + Gate-Entfernung bleiben ADR-Bruch), `govulncheck`-Lauf (Go-Code-Scan nicht Teil der grid-gym-Audit-Surface; Schema-Stub bleibt leer), Pre-Commit-Hook (M7+), `expires`-Erinnerung vor Ablauf (Render-Bruch ist Schwelle), Trivy-Version-Pin-Bump, Cross-Scanner-Konvergenz (M7+). Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0041 + ADR 0042 + ADR 0043; Pattern analog M5-Welle-7-C1 `62f988d`). |
| 0045 | [HTTP-API-Request-Body-Strict-Validation (M6 Welle 5b)](0045-http-api-request-strict-validation.md) | Accepted    | 2026-06-07  | M6-Welle-5b-C1 `cee5aab` (Erstwurf `Proposed`) → `Provisional` mit M6-Welle-5b-C3 (dieser Commit) nach C2-Code-Merge `b580840` (NEU `_BaseRequest`-Mixin + `RunCreateRequest`-Konsolidierung + 11 Integration-Smokes; `make ci`/`make fullbuild` cache-frei gruen). Schaerft ADR-0037 (HTTP-API-Surface-Pattern; `Accepted`) per ADR-0011-Pattern: separate Schaerfungs-ADR ohne Supersedes; ADR 0037 §2.1 + §2.2 + §2.3 bleiben textlich unveraendert. **§2.1** Pflicht-Substanz fuer REST-Request-Bodies: `model_config = ConfigDict(strict=True, extra="forbid")` via NEU `_BaseRequest`-Mixin in `_schemas.py`; alle Request-Modelle (`ControlRequest`, `FaultInjectionRequest`, `RunCreateRequest`) erben. Per-Endpunkt-Bypass via `strict=False`/`extra="allow"` ist ADR-Bruch. **§2.2** Response-Bodies bleiben unveraendert (Default-Pydantic-Mode; Server kontrolliert Felder; Lastenheft-`GG-SAFE-008` adressiert Eingaben). **§2.3** WebSocket-Subscribe-Inputs bleiben aus dem Strict-Vertrag (Subscribe-only-Welle-3/4b-Substanz; kein Client-Payload-Pfad in den Kern); zukuenftiger Bidi-Pfad braeuchte ADR-Schaerfung. **§2.4** `RunCreateRequest`-Konsolidierung: das Modell wandert in C2 aus `app.py` nach `_schemas.py`, damit Strict-Mode-Substanz uniform gilt. Erst-Anwendungsfall: `GG-SAFE-008`-MUSS-Akzeptanz fuer REST-Eingaben in M6-Welle-5b-C2 (NEU 11 Smoke-Tests; davon `test_safe_008_rest_extra_field_rejected` + `test_safe_008_rest_type_coercion_rejected` als Vertrags-Belege). Out-of-Scope: Strict-Mode an Response-Bodies, WebSocket-Client-Payload-Validation, `run_id`-UUID-Path-Param-Validation (FastAPI-Routing-Surface; Welle-5b-D-3 Inline-Fix-Material), Adapter-Side-Input-Validation (Welle-5b-D-4 Audit-Tabellen-Belegung, nicht Pydantic-Vertrag), Custom-Validators (orthogonal zu `model_config`-Mode), Pydantic-Version-Pin-Bump (`pydantic>=2.13` unangetastet; Strict-Mode + extra-forbid seit Pydantic 2.0). Status-Pfad: `Proposed` (dieser Commit) → `Provisional` (M6-Welle-5b-C3 nach C2-Code-Merge; Pattern analog ADR 0038/0039/0040) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0041 + ADR 0042 + ADR 0043 + ADR 0044; Pattern analog M5-Welle-7-C1 `62f988d`). |
| 0046 | [Multi-Python-Test-Stage-Pattern fuer Library-Compat-Smokes (M6 Welle 6)](0046-multi-python-test-stage-pattern.md) | Accepted    | 2026-06-08  | M6-Welle-6-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Schaerft das ADR-0002-Multi-Stage-Dockerfile-Build-Pattern per ADR-0011-Pattern (additive Erweiterung, kein Supersedes); ADR 0002 §2 Sprach-Floor (`requires-python = ">=3.13"`, 3.14 Referenz-Runtime) + §A-1 bleiben textlich unangetastet. **§2.1** Dockerfile-Multi-Python-Stage-Pattern: eine Library-Compat-Test-Stage ist eine additive `--target`-Stage auf einer gepinnten, von der `base`-Stage abweichenden Python-Version (eigener `FROM python:<compat>-slim`-Basis-Layer, NICHT von `base`/`source` abgeleitet); Default-Gate-Stack bleibt auf `ARG PYTHON_VERSION` (3.14); Stage ist opt-in, nicht in der Default-`source`-Kette. **§2.2** Library-Compat-Install-Form: `python -m pip install --ignore-requires-python --no-deps -e .` + `python -m pip install <set>` statt `uv sync --frozen` (Floor-Auflage + Lockfile-Resolution wuerden auf der Compat-Version brechen; `pip` statt `uv pip`, weil nur pip `--ignore-requires-python` kennt); Scope ausschliesslich Compat-Stage — Default-Runtime-/Build-Pfad bleibt `uv sync --frozen`, `--ignore-requires-python` im Default-Pfad bleibt verboten; `pyproject.toml` + `uv.lock` werden NICHT editiert. Erst-Anwendungsfall: Trigger 009 (IEC-61850-In-Process-Smoke-Reaktivierung Pfad B; `pyiec61850-ng` 1.6.1.2 `manylinux1_x86_64`-Wheel segfaultet im SWIG-Layer auf Python >=3.13, siehe ADR 0035 §2.5) — NEU Dockerfile-Stage `iec61850-test` auf `python:3.12-slim` via `make test-iec61850`, geliefert in M6-Welle-6-C2. Pattern-Verallgemeinerung: jede C-Extension-/SWIG-Library mit cp-Tag-Bruch (z. B. spaeter asyncua) folgt derselben `<lib>-test`-Stage-Form ohne neuen ADR. Out-of-Scope: Floor-Senkung auf 3.12, `--ignore-requires-python` im Default-Pfad, Library-Upgrade-Pfad (Trigger-009-Pfad-A bleibt bevorzugt sobald cp314-Wheel verfuegbar), konkrete Compat-Dep-Set-Pins (C2-Detail), separates physisches Compat-uv-Lockfile (M7+), aarch64-Wheel-Support. Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M6-Welle-7-Closure-C1 gebuendelt mit ADR 0041 + ADR 0042 + ADR 0043 + ADR 0044 + ADR 0045; Pattern analog M5-Welle-7-C1 `62f988d`). |
| 0047 | [TelemetrySinkPort Zeitreihen-Persistenz (M7 Welle 1a)](0047-telemetry-sink-timeseries-persistence.md) | Accepted    | 2026-06-08  | M7-Welle-1a-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Schliesst die `GG-PERSIST-001`-Zeitreihen-Persistenz-Lücke von `GG-MVP-002` (erste der zwei Lücken; ReplaySource-Integration ist Welle 1b, per 1b-a-D-1 sub-sliced in ADR 0048 `ReplaySnapshotPort` [1b-a] + ADR 0049 Replay-Lifecycle [1b-b]). Schaerft das ADR-0039-`RunRepositoryPort`-Driven-Persistenz-Pattern additiv (ADR-0011, kein Supersedes). **§2.1** NEU `TelemetrySinkPort` (Driven-Protocol) mit append-only `persist(Sequence[TelemetryPoint])` (Batch pro Tick) + `read_ordered(run_id)` (deterministisch sortiert nach `(simulation_time, sequence)`); nutzt Core-Domain-`TelemetryPoint` (AC-PORTS-NO-OUT-konform, Praezedenz `RunRepositoryPort`). **§2.2** NEU Alembic-`0002_create_telemetry_points.py` (Spalten = `TelemetryPoint`-Felder; `value` als **`TEXT`** mit `str(Decimal)` — NICHT `NUMERIC` wegen Scale-Normalisierung; Sort-/Tie-Index `(run_id, simulation_time, sequence)`; append-only). **§2.3** Sink-Wiring im **Core-Spine** (NEU keyword-only `TickLoop.telemetry_sink`-Kwarg, `None`-no-op; persistiert `TickResult.emitted_telemetry` pro Tick) — NICHT Adapter-Tee, weil der Sink ein **Driven**-Port ist (core-invoked wie `RunRepositoryPort`; der adapter-seitige Live-Stream ist `TelemetryStreamPort`/**Driving**, andere Kategorie). **§2.4** Byte-Stabilitaets-Vertrag (`str(Decimal)`-Round-Trip + stabile Tie-Sortierung) als Welle-1b-`diff_replay()`-Vorbedingung. Out-of-Scope: `ReplaySnapshotPort` (Welle 1b-a, ADR 0048); Lifecycle-Hook + `replay_diff_status`-Metrik + `GG-TERM-002/003`-Preflight (Welle 1b-b, ADR 0049); Query-/Export-API, Batch-/Async-Flush-Tuning, harter FK (spaeter). Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M7-Welle-X-Closure, gebuendelt mit ADR 0048 + ADR 0049). |
| 0048 | [ReplaySnapshotPort Replay-Snapshot-Rekonstruktion (M7 Welle 1b-a)](0048-replay-snapshot-port-reconstruction.md) | Accepted    | 2026-06-09  | M7-Welle-1b-a-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Liefert die Persistenz-Lese-Substanz fuer die zweite `GG-MVP-002`-Lücke (ReplaySource-Integration); zweites Sub-Slice von M7-Welle-1, erstes Sub-Sub-Slice von Welle 1b (1b-a-D-1). Schaerft das Driven-Lese-Port-/Postgres-Pattern additiv (ADR-0011, kein Supersedes); liest die 1a-`telemetry_points` (ADR 0047). **§2.1** NEU `ReplaySnapshotPort` (Driven read-only Protocol) mit `read_samples(run_id) -> tuple[ReplaySample, ...]` (Core-Domain-`ReplaySample`, AC-PORTS-NO-OUT-konform); getrennt von `TelemetrySinkPort` + NICHT `SnapshotPort`/Driving (Schichten-Twist-Vermeidung). **§2.2** Snapshot→`ReplaySample`-Rekonstruktion: `PostgresReplaySnapshotAdapter` liest `telemetry_points` (`ORDER BY id`), mappt pro Zeile; `value` via verlustfreiem `Decimal(TEXT)`-Round-Trip, `import_sequence` via 0-basierter `id`-Order-Enumeration. **Timestamp-Derivations-Vertrag (D-1.1-Lücke):** `ReplaySample.timestamp = str(simulation_time)` — deterministisch aus `simulation_time`, **NICHT** Wall-Clock `started_at` (sonst byte-instabiler Self-Replay; `GG-REPLAY-002` „unveraendert gespeichert" = kanonischer `simulation_time`-String fuer simulations-erzeugte Laeufe). **§2.3** **Keine** neue Tabelle/Migration — liest 1a-`telemetry_points` (Alembic-Head bleibt `0002`; Gruppenplan-R1-Mitigation); reiner Driven-Adapter, **kein** Core-Change (`arch-check` belegt `hexagon/core/**` unveraendert); Lifecycle-Wiring ist 1b-b. Out-of-Scope (Welle 1b-b/ADR 0049): Lifecycle-Hook, `replay_diff_status`-Metrik, `GG-TERM-002/003`-Preflight; volle Equality-Matrix = Trigger 038; externe Replay-Quellen mit echtem Original-`timestamp` = spaetere additive Schaerfung. Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M7-Welle-X-Closure, gebuendelt mit ADR 0047 + ADR 0049). |
| 0049 | [Replay-Lifecycle: Terminal-Hook + `replay_diff_status` + GG-TERM-Preflight (M7 Welle 1b-b)](0049-replay-lifecycle-finalize-hook.md) | Accepted    | 2026-06-09  | M7-Welle-1b-b-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Schliesst die zweite `GG-MVP-002`-Lücke (deterministische E2E-Replay-Verkabelung) + flippt `GG-MVP-002` + `GG-SAFE-006`; drittes/letztes Sub-Slice von Welle 1b (Closure). Schaerft ADR 0039 (`control_state`/`request`) + ADR 0024 (`MetricsPort`/`LogPort`) additiv (ADR-0011, kein Supersedes); konsumiert ADR 0048 (`ReplaySnapshotPort`) + ADR 0047 (Persistenz). **§2.1** NEU idempotente Core-`TickLoop.finalize()`-Terminal-Naht — Driver triggert am Loop-Exit, traegt aber KEINE Diff-Logik (Spine-invoked, [`GG-AR-P-003`](../../../spec/architecture.md#2-architekturprinzipien)/007); `control_state` unveraendert (`completed` bleibt nicht-auto-gesetzt). **§2.2** Referenz-Bindung via NEU keyword-only Kwargs `replay_snapshot` + `replay_reference_run_id` (`None` → no-op); `expected = read_samples(reference)`, `actual = read_samples(current)`; KEIN Self-Replay, KEINE implizite Auto-Auswahl. **§2.3** `GG-TERM-002/003`-MVP-Preflight ueber 5 vorhandene `RunMetadata`-Felder (`scenario_hash`/`schema_version`/`seed`/`tick_ms`/`tool_version`) vor dem Diff; Mismatch → Reject (kein `replay_diff_status`, strukturierter `log_port`-Record), per-Feld-Boundary-Tests; volle Matrix = Trigger 038. **§2.4** `replay_diff_status` binaerer `gauge` (`1.0` clean / `0.0` diverged, `attributes={run_id, reference_run_id, status}`), nur bei preflight-validem Vergleich, keine neue `MetricsPort`-Methode. **§2.5** `GG-SAFE-006`-Detail-Evidence (path/expected/actual/tick/device_id/classification) maschinenlesbar via `log_port` → flippt `docs/user/safe-005-006-…` ⚠ → ✓ + Trigger 036 → `done/`. **§2.6** synchron in `finalize()` (Demo-/Abnahme-Skala; async = additive Schaerfung). Out-of-Scope: oeffentliche API-Replay-Bedienung = Trigger 039; volle GG-TERM-Matrix = Trigger 038; `started_at`/`ended_at`-Setzen + Auto-`completed` + Severity-Stufen + async Diff + `GG-REPLAY-004..006` = spaeter. Status-Pfad: `Proposed → Provisional` (dieser Commit) → `Accepted` (M7-Welle-X-Closure, gebuendelt mit ADR 0047 + ADR 0048). |

| 0050 | [AC-ADAPTER-PURE Bridge-Rueckbau fuer HTTP-Demo-Wiring](0050-adapter-pure-bridge-retirement.md) | Accepted    | 2026-06-09 | Schaerft ADR 0002 A-1 und ADR 0039 additiv: bestehende `AC-ADAPTER-PURE`-`ignore_imports` fuer HTTP-Demo-/Run-Wiring werden nicht erweitert, sondern ueber adapter-sichtbare Domain-/Driving-Port-Surfaces und eine explizite Composition Root ausserhalb `grid_gym.adapters` zurueckgebaut. Umsetzung [`041-adapter-pure-ignore-imports-rueckbau.md`](../planning/in-progress/041-adapter-pure-ignore-imports-rueckbau.md) (M8-Welle-1, C1..C3b): 8 → 0 `ignore_imports`, `Accepted` 2026-06-13 (C3b-Inversion via `ADR 0054`). |
| 0051 | [Fault-Engine-Standort und Adapter-Begriffsklaerung](0051-fault-engine-location-and-naming.md) | Accepted    | 2026-06-09 | Schaerft ADR 0022/0025 additiv: `BatteryFaultAdapter` und `GridFaultAdapter` bleiben fachlich unter `hexagon/core/faults` und werden als Core-Fault-Engines verstanden; ein Move nach `adapters/driven/fault_*` braucht eine neue externe Boundary und Folge-ADR. Umsetzung [`042-fault-engine-location-and-naming.md`](../planning/in-progress/042-fault-engine-location-and-naming.md) (Slice 042 Done 2026-06-13): Rename `*FaultAdapter` → `*FaultEngine`, keine Compat-Aliase, Standort `hexagon/core/faults`. |
| 0052 | [`max_age`-basierte `STALE`-Quality-Stage im TickLoop-Spine (M7 Welle 3a)](0052-max-age-stale-quality-stage.md) | Accepted    | 2026-06-11 | M7-Welle-3a-C1 (direkter `Proposed → Provisional`-Sprung). Schliesst die `GG-SAFE-004`-Lücke (Trigger 034, M6-Welle-5a-Audit ✗) — Emissions-Substanz fuer den seit M5-Welle-6b existierenden `Quality.STALE`-Enum-Wert. Schaerft ADR 0021 (`build_tick_loop`) additiv (ADR-0011, kein Supersedes); Kwarg-Praezedenz ADR 0047 §2.3 + ADR 0049 §2.2/§2.7. **§2.1** NEU keyword-only `TickLoop`-Kwarg `max_age_ms: int \| None = None` (+ `build_tick_loop`-Symmetrie; `None` → Stage aus; `<= 0` → typisierter Reject); BEWUSST kein Scenario-Schema-Feld (`asdict`-`scenario_hash`-Flip wuerde `EXPECTED_DEMO_*`-Pins brechen; Folgeoption = additive Schaerfung mit bewusstem Pin-Update). **§2.2** Stage-Naht in `tick()` unmittelbar vor dem `TickResult`-Bau — eine Stelle, drei Konsumenten (Stream + Persistenz + Replay); Quality-Austausch via `dataclasses.replace`; Bilanz-Abgrenzung (Review-Folge F2): `bucket_sums`-Aggregation laeuft VOR der Stage und bleibt quality-agnostisch (Markierung ≠ Filterung). **§2.3** Severity-Override: `STALE` (3) ersetzt nur `VALID`/`ESTIMATED`/`LIMITED` (0..2); `FAULT_INJECTED`/`INVALID`/`NAN`/`MISSING` (4..7) dominieren. **§2.4** nur Sim-Zeit-Vergleich (`AC-NO-TIME` gewahrt); Demo-Wiring bleibt `None`. **§2.5** Grenzwert strikt `>`. Review-Folge F1 ergaenzt `from_snapshot`-Resume-Symmetrie. Out-of-Scope: Scenario-Feld + per-Geraet/per-Metric + Demo-Aktivierung = additive Schaerfungen; `STALE`-Alarm + Comm-Failure-`MISSING` = Welle 3b (ADR 0053). Status-Pfad: `Accepted` mit M7-Welle-X-Closure (gebuendelt mit ADR 0047..0049). |
| 0053 | [Comm-Failure-Wrapper: Read-Fehler → `MISSING`-Quality + `adapter_communication_lost`-Alarm (M7 Welle 3b)](0053-comm-failure-wrapper-missing-quality-alarm.md) | Accepted    | 2026-06-11 | M7-Welle-3b-C1 (dieser Commit; direkter `Proposed → Provisional`-Sprung). Schliesst die `GG-SAFE-003`-partial-Lücke (Trigger 035, M6-Welle-5a-Audit ⚠) — Quality-/Alarm-Folge fuer die seit M4/M5 existierende typisierte Read-Error-Erkennung der fuenf Protocol-Adapter. Schaerft ADR 0030 (Port-Surface) + ADR 0040 (Alarm-Domain) additiv (ADR-0011, kein Supersedes); Wrapper-Praezedenz ADR 0024 §4.5 (`_protocol_otel_wrap.py`). **§2.1** Scope-Lesart: Flip bindet an Adapter-Substanz + Test-Sibling-E2E (Praezedenz SAFE-001-`INVALID`-Emission); fehlender produktiver `read()`-Pfad = dokumentierte Bestand-Grenze, KEIN Carveout. **§2.2** NEU `CommFailureGuardedDeviceProtocolPort` als EIN geteilter Composition-Wrapper (opt-in; statt 5 per-Adapter-Edits); Komposition Comm-Failure aussen / OTel innen. **§2.3** einheitlich `Quality.MISSING` fuer alle `DeviceProtocolPortReadError`-Subklassen (`STALE` braeuchte Last-Value-Cache — bewusst nicht); MQTT-`None` (leere Queue) ≠ Ausfall; `start`/`stop`/`write` Pass-Through fail-fast. **§2.4** Alarm-Vertrag: `code="adapter_communication_lost"` (vierter stabiler Code), `severity="warning"`, Ziel/Startzeit(`clock.now()`)/Ursache; Emission via injizierten `on_alarm`-Callback (kein Driving-Port im Driven-Ring), Best-Effort gegen Nebenkanal-Fehler. **§2.5** Kontext-Injection keyword-only (`run_id` + `ClockPort` + `alarm_id_source` per ADR-0040-Decision-16-Pattern); Adapter bleiben kontext-frei. **§2.6** synthetisierter `MISSING`-Point-Feld-Vertrag (`source="comm_failure.<target>"`; `metric`/`unit` leer — keine Codec-Introspektion). Out-of-Scope: produktiver `read()`-Pfad, `start()`-/`write()`-Mapping, Retry/Reconnect, `STALE`-Variante, per-Adapter-Severity, F4-Helper-Lift (3b-D-7). Status-Pfad: `Accepted` mit M7-Welle-X-Closure (gebuendelt mit ADR 0047..0052). |
| 0054 | [Composition-ASGI-Entrypoint und Scenario-Hook-Inversion](0054-composition-asgi-entrypoint-and-scenario-hook.md) | Accepted    | 2026-06-13 | M8-Welle-1-041-C3b. Schaerft `ADR 0050` §2.5: `app.py` importiert das Scenario-Bootstrap nicht mehr, sondern registriert den Konfigurator per `_`-Hook (fail-closed Default); NEU `grid_gym.composition.asgi`-Entrypoint verdrahtet `configure_scenario_demo_run` + registriert beim Import. uvicorn-Target (`Dockerfile`-ENTRYPOINT, `__main__`, Env-Smokes) → `composition.asgi:app`. Damit `ignore_imports = []` (0 Bruecken) → `ADR 0050` `Accepted`. `app.py`-Hook ist `_`-prefixt (`AC-NO-GOD-UTILS` max 5 public). Verifiziert per `make fullbuild` (Compose-Smoke ueber neuen Entrypoint). |
| 0055 | [EV-Charger-Device-Pattern (M8 Welle 2a)](0055-ev-charger-device-pattern.md) | Accepted    | 2026-06-13 | M8-Welle-2a (Design C1 + Umsetzung C2/C3). Kombiniert `ADR 0014` (Battery-SoC) + `ADR 0017` (GridConnection-Set-Power): NEU `ev_charger`-Geraetemodell (`GG-DEV-015`) mit **Fahrzeug-SoC**, **CC/CV-Ladekennlinie** (linearer Taper ab `cv_phase_start_soc`), **durchgaengigem V2G** (`max_discharge_kw > 0`, hart bei `soc=0` gestoppt), Stecker-Zustand + **EV-Fault** (NEU `FAULT_TYPE_CONNECTION_LOSS`, `FaultInjectableDevice`). Acceptance `GG-DEV-015` (Modell + `ev_charger_demo.yaml` + deterministischer Smoke + Snapshot-Roundtrip) geliefert; `Accepted` 2026-06-13 (Welle 2a-C2/C3, §7 Fitness-Functions, `make gates` gruen). Trigger 016 aufgeloest. Out-of-Scope: exponentielle Kennlinie, Multi-EV, ISO 15118/OCPP, scenario-getriebene EV-Fault-Engine. |
| 0056 | [Transformer-Device-Pattern (M8 Welle 2b)](0056-transformer-device-pattern.md) | Accepted    | 2026-06-14 | M8-Welle-2b (Design C1 + Umsetzung C2/C3). Folgt dem `ADR 0017` (GridConnection-Set-Power)-Muster: NEU `transformer`-Geraetemodell (`GG-DEV-016`) mit **Wandlungsverhaeltnis** (`turns_ratio`), **Eisen-/Leerlaufverlust** (konstant) + **Kupfer-/Lastverlust** (quadratisch im Lastfaktor), **Saettigungs-Hard-Cap** bei `rated_power_kw` + **Schutz-Fault** (NEU `FAULT_TYPE_WINDING_FAULT`, `FaultInjectableDevice` → Sekundaer/Verlust hart `0`). Acceptance `GG-DEV-016` (Modell + `transformer_demo.yaml` + deterministischer Smoke + Snapshot-Roundtrip) geliefert; `Accepted` 2026-06-14 (Welle 2b-C2/C3, §7 Fitness-Functions, `make gates` gruen). Trigger 017 aufgeloest. Out-of-Scope: Tap-Changer, Wicklungstemperatur/Alterung, weiche Saettigung, Netzbilanz-Grenzen (Trigger 021). |
| 0057 | [Wind-Turbine-Device-Pattern (M8 Welle 2c)](0057-wind-turbine-device-pattern.md) | Accepted    | 2026-06-14 | M8-Welle-2c (Design C1 + Umsetzung C2/C3). Folgt dem `ADR 0016` (PV)-Muster: NEU `wind_turbine`-Geraetemodell (`GG-DEV-017`) mit **kubischer Leistungskennlinie** (cut-in/rated/cut-out) und **stochastischem seeded `RandomPort`-Windeingang** (erster echter `RandomPort`-Konsument; `ADR 0007`). Kein Command/Alarm/Fault (command-loser Generator). `min == max` = konstanter Wind (RNG-frei testbar). Acceptance `GG-DEV-017` (Modell + `wind_turbine_demo.yaml` + deterministischer Smoke) geliefert; `Accepted` 2026-06-14 (Welle 2c-C2/C3, §7 Fitness-Functions, `make gates` gruen). Trigger 018 aufgeloest. Out-of-Scope: Windprofil-Eingang, geglaettete Kennlinie, Wakes/Aerodynamik, Turbinen-Schutz-Fault, stand-kontinuierlicher Resume stochastischer Geraete. |
| 0058 | [Diesel-Generator-Device-Pattern (M8 Welle 2d)](0058-diesel-generator-device-pattern.md) | Accepted    | 2026-06-14 | M8-Welle-2d (Design C1 + Umsetzung C2/C3). Folgt dem `ADR 0014` (Battery, endliche Ressource)-Muster: NEU `diesel_generator`-Geraetemodell (`GG-DEV-018`) mit **Kraftstoff-Vorrat** (l) + **Verbrauch** (l/kWh) + **Min-Startleistung** + **Ramp** + **Anfahr-/Abstell-Hysterese** (running-Zustandsmaschine) + **Schutz-Fault** (NEU `FAULT_TYPE_GENSET_FAULT` → Leistung/Verbrauch hart `0`). Generation-only; in `_BILANZ_SOURCE_BUCKETS` (`generation`). NEU `snapshot_codec.assert_bool`. Acceptance `GG-DEV-018` (Modell + `diesel_demo.yaml` + deterministischer Smoke + Snapshot-Roundtrip) geliefert; `Accepted` 2026-06-14 (Welle 2d-C2/C3, §7 Fitness-Functions, `make gates` gruen). Trigger 019 aufgeloest. Out-of-Scope: Emissionen, Wartung, Nachtanken, Inselnetz-Bilanz (Trigger 020). |

---

## Lese-Reihenfolge bei Drift

Wenn Code, Tests oder Slice-Plaene auf einen Vertrag referenzieren,
der in einer aelteren `Accepted`-ADR steht, **immer pruefen, ob
eine Folge-ADR in der „Schaerfungen"-Spalte oben die Stelle
schaerft.** Im Zweifel:

1. Folge-ADR lesen — sie traegt die maßgebliche Fassung.
2. Original-ADR-Stelle bleibt historisch (kein Edit per
   `ADR 0006 §3`).
3. Adapter-/Modul-Code-Docstrings zitieren beide ADRs ueber den
   `ADR NNNN`-Tag (siehe z. B.
   `src/grid_gym/adapters/driven/random_mt/mersenne_twister.py`).

---

## Konvention

- Neuer ADR-Eintrag in dieser Tabelle ist Pflicht bei jeder neuen
  ADR. Reihenfolge: aufsteigend nach Nummer.
- Wenn eine ADR eine andere abloest oder schaerft, wird die
  „Schaerfungen"-Spalte der **alten** ADR aktualisiert. Die alte
  ADR selbst bleibt textlich unveraendert (per ADR 0006 §3).
- Statuswechsel (z. B. `Provisional → Accepted`) werden in der
  ADR-Datei selbst dokumentiert (Header-Pflichtfelder per
  `ADR 0006 §4`); diese Tabelle reflektiert sie.
