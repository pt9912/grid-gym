# ADR-Index — grid-gym

Lebende Uebersicht ueber alle ADRs, ihren Status und die
Vorrang-/Schaerfungs-Beziehungen zwischen ihnen. Diese Datei ist
**kein** ADR-Entscheidungstext — sie ist eine Service-Notiz fuer
Reviewer, damit Drift zwischen Accepted-Texten (die per
`ADR 0006 §3` immutable sind) und nachgelagerten Folge-ADRs
sichtbar bleibt.

Reihenfolge: aufsteigend nach ADR-Nummer. Eine ADR mit Schaerfung
durch eine Folge-ADR traegt eine Spalte „Schaerfungen" mit
Verweisen — die Folge-ADR ist verbindlich, der Original-Text
historisch.

---

## Aktive ADRs

| ADR  | Titel                                                           | Status      | Datum       | Schaerfungen / Folge-ADRs                                                                                          |
| ---- | --------------------------------------------------------------- | ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------ |
| 0001 | [Dokumentations- und Planungsstruktur](0001-documentation-and-planning-structure.md) | Accepted    | 2026-05-13  | —                                                                                                                  |
| 0002 | [Sprach- und Build-Stack](0002-language-and-build-stack.md)     | Accepted    | 2026-05-14  | [`ADR 0008`](0008-enum-as-domain-frozen-form.md) erweitert §A-1 AC-DOMAIN-FROZEN um Enum-Subklasse als dritte Form; [`ADR 0029`](0029-no-coverage-pragma-contract.md) ergaenzt §A-1 um den 11. `tools/arch_check.py`-Contract `AC-NO-COVERAGE-PRAGMA` (Schaerfung-ohne-Supersedes per ADR 0011, M3-Welle-5-Hygiene-Folge). |
| 0003 | [ADR-Lifecycle](0003-adr-lifecycle.md)                          | Superseded  | 2026-05-13  | Abgeloest durch [`ADR 0006`](0006-adr-lifecycle-superseding-and-process-corrections.md)                            |
| 0004 | [Kennungs-basierte Querverweise](0004-identifier-based-cross-references.md) | Accepted    | 2026-05-13  | —                                                                                                                  |
| 0005 | [Type-Check-Gate (`mypy --strict`)](0005-type-check-gate.md)    | Accepted    | 2026-05-15  | —                                                                                                                  |
| 0006 | [ADR-Lifecycle, Superseding und Prozess-Korrekturen](0006-adr-lifecycle-superseding-and-process-corrections.md) | Accepted    | 2026-05-15  | **§3-Aenderungsregeln** geschaerft durch [`ADR 0011`](0011-schaerfung-ohne-abloesung.md): „Schaerfung ohne Supersedes" als zulaessige Folge-ADR-Form. Zusaetzlich geschaerft durch [`ADR 0028`](0028-link-maintenance-accepted-adr-bezug.md): Pfad-Updates an Markdown-Hyperlink-URLs in `Bezug:`-Linien sind keine fachliche Aenderung (Forwarder-Stub-Pattern abgeloest durch direkte Pfad-Pflege). |
| 0007 | [`RandomPort`-Implementierung](0007-random-port.md)             | Accepted    | 2026-05-15  | **§5.2-Snapshot-Schema** abgeloest durch [`ADR 0009`](0009-randomport-snapshot-schema-rng-version.md): `state`-Feld → `rng_version` + `rng_state`. **§5.1-Protocol-Erweiterung** durch [`ADR 0010`](0010-randomport-snapshot-as-mapping.md): zusaetzliche Methode `snapshot_as_mapping()`. Beide reine Erweiterungen, kein Supersedes. |
| 0008 | [Enum-Subklassen als AC-DOMAIN-FROZEN-Form](0008-enum-as-domain-frozen-form.md) | Provisional | 2026-05-17  | Erweitert `ADR 0002 §A-1` AC-DOMAIN-FROZEN; Acceptance synchron zur M1-Welle-1-PR-Mergung                          |
| 0009 | [`RandomPort`-Snapshot-Schema](0009-randomport-snapshot-schema-rng-version.md) | Accepted    | 2026-05-17  | Erweitert `ADR 0007 §5.2`; reine Erweiterung, kein Supersedes                                                       |
| 0010 | [`RandomPort.snapshot_as_mapping`](0010-randomport-snapshot-as-mapping.md) | Accepted    | 2026-05-17  | Erweitert `ADR 0007 §5.1` + `ADR 0009`: Composition-API fuer `SnapshotEnvelope`. Single-Source-of-Truth `_build_payload()`. Reine Erweiterung, kein Supersedes. |
| 0011 | [Schaerfung durch parallele ADR ohne Supersedes](0011-schaerfung-ohne-abloesung.md) | Accepted    | 2026-05-17  | Schaerft `ADR 0006 §3`: dokumentiert die „Schaerfung ohne Supersedes"-Form, die ADR 0008/0009/0010 bereits implizit nutzen. Self-bootstrap (ist selbst dieses Muster). |
| 0012 | [API + Simulation als zwei Prozesse](0012-api-simulation-two-processes.md) | Accepted    | 2026-05-17  | Schliesst `GG-AR-OPEN-002` (spec/architecture.md §19): api + simulation als zwei Prozesse, Postgres als Persistenz-Bus. Welle 6c hat den Pattern bereits implementiert; ADR formalisiert nachtraeglich. |
| 0013 | [`DeviceModel`-Protocol als Core-internes Protocol](0013-device-model-protocol.md) | Accepted    | 2026-05-18  | `DeviceModel`-Vertrag fuer M2-Geraete (`GG-DEV-001..003`). §2.4 `from_snapshot`-Pflicht schaerft durch [`ADR 0014`](0014-battery-snapshot-schema.md) (Battery), [`ADR 0016`](0016-pv-load-device-pattern.md) (PV+Load), [`ADR 0017`](0017-grid-connection-device-pattern.md) (GridConnection), [`ADR 0018`](0018-smart-meter-device-pattern.md) (SmartMeter) — alle reine Erweiterungen je Geraet, kein Supersedes. |
| 0014 | [Battery-Snapshot-Schema + Command-Surface (M2 Welle 2)](0014-battery-snapshot-schema.md) | Accepted    | 2026-05-18  | Vorlage fuer das Geraete-Snapshot-/Command-Pattern. Schaerft `ADR 0013 §2.4` fuer den Battery-spezifischen Snapshot-Vertrag (`Schaerfung ohne Supersedes`-Pattern, ADR 0011). §§3/7 cross-referenziert durch [`ADR 0016`](0016-pv-load-device-pattern.md) (`set_mode` ist Welle-5+-Material). |
| 0015 | [TickLoop-Snapshot-v2 im SnapshotEnvelope-Pattern (M2 Welle 6a)](0015-snapshot-envelope-v2.md) | Accepted    | 2026-05-20  | Welle-6a-TickLoop-Snapshot-Bump: `TickLoop.snapshot()["version"]` von `1` (M1-Welle-4-Stand) auf `2`; der generische `SnapshotEnvelope`-Konstruktor bleibt tolerant. Strukturierender Bruch — der Inhalts-Vertrag des `sub_snapshots`-Mapping erweitert sich um `devices.<device_type>.<device_id>` x 5 und `grid_model`. v1-Read in M2-Code-Pfaden wirft typisierten `TickLoopSnapshotVersionError` (mit M6-`GG-PERSIST-*`-Pointer in der Message). Unbekannte Sub-Snapshot-Keys werden toleriert (Forward-Compat fuer Welle 7+ `agents.*` o.ae.). Schreib-Pfad-Exception `TickLoopUnknownDeviceTypeError` fuer fehlende `_DEVICE_TYPE_BY_CLASS_NAME`-Eintraege. §4-Forward-Pointer fuer Welle-7+/M3-`device_type`-Protocol-Property. Status-Uebergang: `Proposed → Provisional` (Welle 6a-Merge) → `Accepted` (Welle-6c-Closure `c31052c`, MVP-Demo exerziert v1→v2 + typisierten Error end-to-end). |
| 0016 | [PV + Load Generation/Consumption-Device-Pattern (M2 Welle 3)](0016-pv-load-device-pattern.md) | Accepted    | 2026-05-18  | Gemeinsame ADR fuer PV (`GG-DEV-011`) und Load (`GG-DEV-013`). Spiegelt das ADR-0014-Pattern in einfacherer Form (kein SOC / Ramp / Wirkungsgrad). §2.2 Sign-Konvention bindet [`ADR 0017`](0017-grid-connection-device-pattern.md) §2.2 (gleiche Netzbilanz-Formel) und [`ADR 0018`](0018-smart-meter-device-pattern.md) §2.4 (Aggregation respektiert Quellen-Sign). |
| 0017 | [GridConnection-Anschlusspunkt-Pattern (M2 Welle 4a)](0017-grid-connection-device-pattern.md) | Accepted    | 2026-05-19  | Welle-4a-Geraet `GG-DEV-012`. Stateful Anschlusspunkt mit kumulativem `import_kwh`/`export_kwh`. Sign-Konvention `power_kw > 0 = Import` schliesst die Welle-5-Netzbilanz-Formel aus [`ADR 0016`](0016-pv-load-device-pattern.md) §2.2. Separate ADR statt geteilt mit [`ADR 0018`](0018-smart-meter-device-pattern.md) (verschiedene State-Modelle). §§2.4/2.5 geschaerft durch Welle-4a-Review-Folge (Commit `1ed976a`: IGNORED-Klarstellung + Decimal-Praezisions-Note). Status-Uebergang: `Proposed → Provisional` (Welle 4a-Merge) → `Accepted` (Welle-4-Closure). |
| 0018 | [SmartMeter-Aggregator-Pattern (M2 Welle 4b)](0018-smart-meter-device-pattern.md) | Accepted    | 2026-05-19  | Welle-4b-Geraet `GG-DEV-014`. Stateless Aggregator ueber `aggregate_device_ids: tuple[str, ...]` via neuem `attach_sources(...)`-Lifecycle-Hook (Analogie zu Welle-3-Review-M-6-`attach_random`). Snapshot persistiert **keine** Aggregat-Werte (derived). Schwester-ADR zu [`ADR 0017`](0017-grid-connection-device-pattern.md). §2.2 geschaerft durch Welle-4b-Review-Folge (Commit `1093b2c`: `aggregate_metric_name`-Pflicht auf `"power_kw"` in Welle 4b; Welle 5+ aktiviert Forward-Looking). Status-Uebergang: `Proposed → Accepted` direkt (Welle-4-Closure). |
| 0019 | [Grid-Model-Bilanz-Pattern (M2 Welle 5a)](0019-grid-model-bilanz-pattern.md) | Accepted    | 2026-05-19  | Welle-5a-Modul `hexagon/core/grid_model/` (`bilanz.py` + `config.py` + `snapshot.py`). Proportionales Frequenz/Spannungs-Modell `f = f_nom + k_f * imbalance`, `v = v_nom + k_v * imbalance` mit Safety-Clamps. **Kein DeviceModel** — Single-Instance-System-Modell, `grid_model` als Snapshot-Sub-Key (kein `devices.<id>`). Imbalance-Berechnung schliesst die Welle-5-Netzbilanz aus [`ADR 0016`](0016-pv-load-device-pattern.md) §2.2 und [`ADR 0017`](0017-grid-connection-device-pattern.md) §2.2; GridConnection wird in Welle 6 als Bilanz-Schluss-Variable verdrahtet. §§3/5/6 geschaerft durch 3 Pre-Implementation-Runden + Welle-5a-Review-Folge. Welle-5b-Lieferung erweitert das Snapshot-Layout v1→v2 mit Backward-Compat (ADR 0020 ohne Supersedes). Status-Uebergang: `Proposed → Provisional` (Welle 5a-Merge) → `Accepted` (Welle-5-Closure mit ADR 0020). |
| 0020 | [Load-Profile + Load-Event Pattern (M2 Welle 5b)](0020-load-profile-and-event-pattern.md) | Accepted    | 2026-05-19  | Welle-5b-Erweiterung von `hexagon/core/grid_model/` um `loads.py` (`LoadEvent` + `LoadProfile` + pure CSV/JSON-Parser). Schwester-ADR zu [`ADR 0019`](0019-grid-model-bilanz-pattern.md): Welle 5a ist Physik, Welle 5b ist Daten-/I/O-Integration. `GridModelSnapshot` bumpt v1→v2 mit Backward-Compat-Lesepfad (v1-Snapshot ohne LoadEvents/Profiles bleibt roundtrip-faehig). Liefert `GG-GRID-003` (Lasten konstant/Zeitreihe/Event) + `GG-GRID-004` (Lastspruenge). Datei-I/O ist Adapter-Verantwortung (`GG-AR-TABU-002`); §§2.1/2.2/2.3/2.4/2.5/2.6 geschaerft durch 1 Pre-Implementation-Runde + Welle-5b-Review-Folge (Commits `53a4f91` + `47c054a`). M3-Driftrisiko gegenueber Replay-Source-Pfad explizit dokumentiert (M3 baut auf 5b, repliziert nicht). Status-Uebergang: `Proposed → Accepted` direkt (Welle-5-Closure). |
| 0021 | [Scenario-Loader + TickLoop-Event-Wiring + GridConnection-Auto-Schluss (M2 Welle 6b)](0021-scenario-loader-and-tick-loop-event-wiring.md) | Accepted    | 2026-05-20  | Welle-6b-Builder-/Wiring-ADR: `hexagon/core/scenario/loader.py` erweitert um `build_devices(...)` (Factory-Dispatch nach `ScenarioDevice.type`), `build_tick_loop(...)` (produktiver TickLoop-Builder), und `parse_load_events`/`parse_load_profiles`-Funktionen (plus internem `_parse_grid_model_config`). Validator faengt die drei optionalen Top-Level-Sektionen (`grid_model`, `load_events`, `load_profiles`) typisiert ab. TickLoop erhaelt einen Vor-Tick-Block, der LoadProfiles (Baseline) → LoadEvents (Overlay) → GridConnection-Auto-Schluss (`-pre_grid_residual_kw`) auf Devices anwendet (Split-Iteration, half-open Event-Window mit `tick_start_ms`). SmartMeter-`attach_sources`-Aufruf ist Scenario-Loader-Verantwortung nach Device-Konstruktion. Neue typed Exceptions `ScenarioUnknownDeviceTypeError` + `ScenarioMissingSourceDeviceError` + `ScenarioInvalidLoadTargetError`. Plugin-Registry fuer externe DeviceModel-Klassen bleibt M5/M6-out-of-scope. Welle-6b-Review-Folge (Commit `0f1c597`) hat 14 Findings (4 High, 7 Medium, 3 Low, 1 Niedrig) ohne Supersede-ADR adressiert (Schaerfung-Pattern); H-4 Validator-Erweiterung + parse-Helfer schliesst den YAML→TickLoop-Pfad. Status-Uebergang: `Proposed → Provisional` (Welle-6b-Merge, Commit `0f1c597`) → `Accepted` (Welle-6c-Closure `c31052c`, MVP-Demo exerziert Builder + Vor-Tick-Block produktiv ueber ≥ 100 Ticks + Permutations-Property-Test). |
| 0022 | [Fault-Injection-Protocol + Scenario-Validator-Härtung (M3 Welle 1)](0022-fault-injection-protocol.md) | Provisional | 2026-05-20  | M3-Welle-1-Erstwurf: `FaultInjectableDevice(DeviceModel)` Sub-Protocol unter `hexagon/core/faults/_protocol.py` (per ADR 0013 §2.8 — keine Erweiterung der Base-`DeviceModel`-Surface, sondern Closed-Set-Sub-Protocol mit `inject_fault(fault_type, payload)`). Neuer Driven-Port `FaultPort` unter `ports/driven/fault.py` mit `apply_active_faults(devices, context) -> None` (Orchestrierungs-Hook, nicht Per-Device-Hook; Welle-1-Port-Surface ist structural `Sequence[object]` wegen AC-PORTS-NO-OUT — Welle-2-Adapter typisiert intern strenger). Scenario-Validator-Haertung: `_assert_fault_list` prueft Target-Existenz analog `_assert_event_list` (neuer typed `ScenarioUnknownFaultTargetError`); KEINE Type-Whitelist, KEINE semantische Validation (Welle-2-Material). TickLoop-Hook im Vor-Tick-Block nach `_consume_load_inputs_into`, vor erster `_run_device_iteration`; neuer Konstruktor-Kwarg `fault_port: FaultPort \| None = None` (keyword-only, kein produktiver NullFaultPort — Hook skippt bei None). Snapshot-Schema bleibt v2 in Welle 1; Bump v2→v3 wird in Welle 2 oder M6 entschieden. Welle 2 liefert konkrete Adapter (`BatteryFaultAdapter`, `GridFaultAdapter`) + Fault-Typen (`cell_failure`, `voltage_drop`) + Recovery-Logik. Status-Uebergang: `Proposed → Provisional` (M3-Welle-1-Merge `79bb50a`, 773 Unit-Tests + 11 neue Fault-Tests). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.4 (Recovery-Semantik) wird durch [`ADR 0025`](0025-fault-recovery-pattern.md) erweitert (Schaerfung-ohne-Supersedes, ADR 0011-Pattern). |
| 0023 | [Multi-Agent-Bus + Agent-Protocol (M3 Welle 3)](0023-agent-bus-protocol.md) | Provisional | 2026-05-21  | M3-Welle-3-Erstwurf: `Agent`-Sub-Protocol unter `hexagon/core/agents/_protocol.py` (eigenstaendig, **nicht** `DeviceModel`-erbend — Agents stehen neben Geraeten, nicht auf ihnen; ADR 0013 §2.8-konform). `AgentMessageBus` als **Core-Klasse** unter `hexagon/core/agents/bus.py` (Pattern-Drift gegen ADR 0022 `FaultPort`: Architektur §14 schreibt eigenes Kernmodul + AgentMessageBus vor, Architektur §4.2 listet keinen AgentBus-Port, State-/Boundary-Argument — Driven-Ports sind Adapter-Boundary, AgentBus hat keine externe Boundary; `MersenneTwisterRandomPort` ist Gegenbeleg: Port + stateful + externe PRNG-Library, Welle-3-Review-Folge M-2 schaerft die urspruengliche AC-PORTS-NO-OUT-Argumentation, Test-Isolierung via Sub-Protocol). Status-Uebergang: `Proposed → Provisional` (M3-Welle-3-C2-Merge `4fa122d` + Review-Folge `d6f66fc`, 879 Unit-Tests + 14 Integration-Tests + A-1-Gates gruen ohne Override). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen** (Welle-3-Review-Folge, alle Schaerfung-ohne-Supersede per ADR 0011): H-1 (`AgentMessage.payload`-MappingProxyType-Versprechen gestrichen — Konsistenz mit ScenarioFault/Command/Event-Pattern; Domain-weiter Wrap waere Welle-4-Refactor); M-1 (`agent_message.py` aus `CRITICAL_COV_TARGETS` gestrichen — Dockerfile-Stage prueft `[ ! -d "${target}" ]`, Coverage erfasst das Modul trotzdem via `core/agents/bus.py`-Import); M-2 (AC-PORTS-NO-OUT-Argument durch State-/Boundary-Argument ersetzt — `MersenneTwisterRandomPort` als Gegenbeleg fuer "Port + stateful"); M-3 (Sub-Random-Stream-Konvention `agent-{agent_id}` nach Welle 4 verschoben, weil Welle-3-Bus keinen RandomPort-Kwarg hat); M-4 (Welle-4-Pflicht-Eviction-Spec ergaenzt — `consume_for(receiver)` mit Per-Receiver-Watermark oder `evict_before(simulation_time)`; Welle-3-Foundation-Buffer waechst sonst unbegrenzt). Plus L-1..L-4 als Code-Schaerfungen (`_set_agents_for_testing`-Helper am TickLoop mit Agent-ID-Eindeutigkeits-Check; `AgentBusInvalidSequenceError` fuer `publish(sequence < -1)`; `AgentBusInvalidReceiverError` fuer `drain_for("*")`; `AgentBusError`-Welle-3-Surface-Klarstellung). | `AgentMessage`-frozen-dataclass unter `hexagon/core/domain/agent_message.py` mit `GG-AGENT-004`-Pflicht-Feldern. TickLoop-Hook zwischen Schritt D und E (Architektur §6 Schritt 7); Agent-Commands gehen in den Scheduler und werden in der **naechsten** Tick wirksam (GG-AGENT-008 Commit-Reihenfolge-Invariante). Neuer Konstruktor-Kwarg `agent_bus: AgentMessageBus \| None = None` (keyword-only, kein produktiver NullBus — Hook skippt bei None); `build_tick_loop`-Builder-Symmetrie analog ADR 0021 §2.4. Observability-Vorgriff-Verbot: AgentBus/Agent injizieren **keine** LogPort/MetricsPort/TracePort in Welle 3 (ADR 0024 entscheidet Wiring in Welle 5/6). Trigger 011 (Sub-Seed-Wortbreite) bleibt in `open/` — Welle-3-Skala (< 100 Agents) erreicht Aktivierungs-Schwelle (`> 10⁶ Sub-Ports`) nicht. Snapshot-Vertrag: AgentMessageBus haelt eigene `snapshot()`-Surface, wird aber in Welle 3 nicht in `TickLoop.snapshot()` eingehaengt; Welle 4 fuegt Sub-Snapshot-Slot `agent_bus` + `agents.<type>.<id>` additiv per ADR 0015 §2.3 ein. Welle 4 liefert konkrete `Agent`-Implementer (`RuleBasedAgent` o. ae.), Agent-Registry, Decision-Logik, `agents`-Top-Level-Block im Scenario-Schema. Status-Uebergang: `Proposed → Provisional` mit M3-Welle-3-C2-Merge (TBD). `Accepted` mit M3-Welle-7-Closure. |
| 0024 | [Observability-Port-Trio: LogPort + MetricsPort + TracePort (M3 Welle 5)](0024-observability-port-trio.md) | Provisional | 2026-05-23  | M3-Welle-5-Erstwurf: Driven-Port-Trio `LogPort`/`MetricsPort`/`TracePort` als `Protocol` (`@runtime_checkable`, stateless, keine Default-Methoden, **keine** OTLP/SDK-Typen im Port-Layer — Core bleibt OTLP-frei). `TracePort` mit internem `SpanContext`-frozen-dataclass (`trace_id`/`span_id`/`parent_span_id` String-basiert); `start_span`/`end_span`/`record_event` mit Pflicht-`SpanContext`-Param + No-Op-Fallback bei `None` als Adapter-Robustheit. Drei Null-Adapter (`adapters/driven/observability_null/`) mit Default-`call_count`+`last_call`-Surface und opt-in `record_calls=True` fuer `call_records`+`clear_calls()` (loest M3-welle-5.md §7 R-2 — Coverage ohne opt-in). Additive Hook-Verdrahtung in TickLoop (Tick-Telemetrie + Logs) / Agent (Decision-Trail) / Fault (Span-Wrap + Audit) — ADR 0022/0023/0026 Schritt-/Atomizitaets-Vertraege unangetastet; `log_port`/`metrics_port`/`trace_port`-Kwargs konstruktor-symmetrisch zu ADR 0022/0023 mit Default `None` (Hook skippt). Loest [`ADR 0023`](0023-agent-bus-protocol.md) §2.6 Observability-Vorgriff-Verbot auf. Trigger 006 (`--strict-bytes`) explizit deferred auf Welle 6 (kein Bytes-Pfad in Welle 5). Status-Uebergang: `Proposed → Provisional` (M3-Welle-5-C2-Merge `718c177`, 1023 Unit-Tests + 19 Integration-Tests, `make fullbuild` cache-frei gruen ohne Override, Coverage 95.55%). `Accepted` mit M3-Welle-7-Closure nach Welle-6-OTLP-Compose-Smoke-Verifikation. **Hygiene-Folge** (`9ae2376`): [`ADR 0029`](0029-no-coverage-pragma-contract.md) `Accepted` (Schaerfung-ohne-Supersede von ADR 0002 §A-1; 11. arch_check-Contract `AC-NO-COVERAGE-PRAGMA`). **Welle-5-Review-Folge** (2026-05-23, Vor-`Accepted`-Schliff per ADR 0006 §4): §1 N-3 (Pre-Tick-Hook-Ref `ADR 0022 §2.5 → §2.4`); §2.6 M-1 (Log-/`tick_duration_ms`-Hooks als Welle-6-Material qualifiziert — Welle-5-Minimum ist Span-Wrap; `AC-NO-TIME` blockiert Wall-Clock im Core); §4.4 M-2 (neu — Welle-6-Forward-Pointer: Sentinel-Pattern fuer `scenario.observability` analog Welle-4b-F-1, Trace-ID-Determinismus via `RandomPort.sub_port` analog Welle-3-M-3, Vertragsschnitt fuer `SpanContext`+Port-Signaturen analog Welle-4b-F-2, plus L-2/N-1/N-2 als Welle-6-Folgearbeit-Bullets); `Bezug:`-Liste L-3 (`ADR 0011` ergaenzt). Code-Inline-Fixes: H-1 (tick_span im try/finally), L-1 (tick_end-Log vor Span-Close), M-3 (Makefile-Help-Text 16→17), L-4 (Test-Asserts `is → ==`). **Welle-6-C1.2-Schaerfung** (2026-05-24, Schaerfung-ohne-Supersede per ADR 0011): §4.5 ergaenzt um acht normative Decisions fuer den Welle-6-OTLP-Adapter — L-2 (Provider-Lifecycle: lazy Provider-Init im Adapter-Konstruktor, OTel-globale `set_logger_provider`/`set_meter_provider`/`set_tracer_provider` werden im `build_otlp_adapters`-Factory gesetzt; `flush_and_shutdown`-Helper fuer geordneten Teardown), N-1 (resource semconv: `service.name`/`service.version`/`deployment.environment` + `service.instance.id` als Pflicht-Felder via `OtlpAdapterConfig`), N-2 (`scenario.observability`-Sentinel-Pattern fuer fehlende Telemetrie-Konfiguration analog Welle-4b-F-1 — `None`-Default propagiert), Sentinel (`scenario.observability is None` → `NullLogAdapter`+`NullMetricsAdapter`+`NullTraceAdapter` ohne Init-Side-Effects), Trace-ID-Determinismus (Span-IDs via `RandomPort.sub_port("trace")` deterministisch in Tests, Production-Pfad delegiert an OTel-SDK), D-4 (Decision-Vertragsschnitt `SpanContext`+Port-Signaturen unveraendert — Adapter ist additiv ohne Core-API-Bruch), gRPC-Pinning (`opentelemetry-exporter-otlp-proto-grpc` als einzig zulaessiger Exporter in Welle 6; `proto-http` als Welle-7+-Material), Smoke-Determinismus (Compose-Smoke-Verifikation in Welle-6-C3 muss `≥1 Span + ≥1 Metric + ≥1 LogRecord` pro Tick-Run beobachten). Neuer 12. arch_check-Contract `AC-OTLP-ADAPTER-NO-TIME` (Schaerfung-ohne-Supersede von ADR 0002 §A-1): verbietet `time.time`/`time.monotonic`/`datetime.now` etc. in `src/grid_gym/adapters/driven/telemetry_otlp/**` (Adapter-interner Code muss Wall-Clock durch `ClockPort` beziehen — sonst Drift gegen TickLoop-Sim-Zeit). |
| 0025 | [Fault-Recovery-Pattern (M3 Welle 2)](0025-fault-recovery-pattern.md)                          | Provisional | 2026-05-20  | M3-Welle-2-Erstwurf: Recovery-Engine fuer Faults. Zwei Recovery-Modi in Welle 2 produktiv: `auto-recover-after-N-ticks` (Default, Adapter zaehlt `remaining_ticks` herunter und clearet Device-Flag nach Window-Ende) + `manual-via-command` (explizites `manual-recover-fault`-Command mit Pflicht-Payload `fault_id` + `target_device_id`, optional `correlation_id`). `permanent`-Modus verschoben auf Welle 3+/M6 (braucht Lauf-uebergreifende Persistenz). Recovery-State-Lokalisation: **Adapter haelt Scheduling** (`_active_faults: dict[(fault_id, target_id), int]`), **Device haelt nur Physik-Flag** (`_cell_failure_active: bool`, `_voltage_drop_active: bool`). `inject_fault`-Idempotenz: Adapter ruft Device nur beim Uebergang inactive→active oder active→inactive (nicht bei unveraendertem Zustand). Recovery-Window-Vertrag: half-open `[start, end)` analog Welle-6b-LoadEvent (ADR 0021 §2.5). Snapshot-Erweiterung: Battery + GridConnection Sub-Snapshots additiv um `fault_state`-Block (ADR 0015 §2.3 erlaubt das ohne v2→v3-Bump; ADR 0014/0017 bleiben `Accepted`). Recovery-Telemetry verschoben auf Welle 5/6 (Observability). Schaerft ADR 0022 §2.4, kein Supersede (ADR 0011-Pattern). Status-Uebergang: `Proposed → Provisional` (M3-Welle-2-Closure `91d44e2`, 840 Unit-Tests + 14 Integration-Tests + A-1-Gates gruen). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen** (Welle-2-Closure-Folge): Property-Tests + Fault-Demo-Integration + Composite-FaultPort-Test-Helper (`tests/integration/_fault_composite.py`, Welle-3-Promote-TODO); Symmetrie-Notiz `build_tick_loop(fault_port=)` in ADR 0021 §2.4 + ADR 0022 §2.5 ergaenzt. |
| 0026 | [Agent-Drain + Registry + Snapshot + Lifecycle (M3 Welle 4a)](0026-agent-drain-registry-pattern.md) | Provisional | 2026-05-21  | M3-Welle-4a-Erstwurf: Schwester-ADR zu ADR 0023 (Welle-3-Foundation); Pattern-Pendant zu ADR 0025 (Welle-2-Konkretisierung). Schaerft ADR 0023 §6 verbindliche Welle-4-Konsequenzen ohne Supersede (ADR 0011-Pattern). Sechs Entscheidungen: §2.1 Drain-Pfad als Pre-Tick-Schritt **A0v** (Target-Validierung vor `clock.advance`/`scheduler.pop_due`) + **A0a** (`apply_command`-Apply nach Clock, vor Schritt A) — Atomizitaets-Vertrag bei `AgentInvalidCommandTargetError`; Agent-Commands auf GridConnection-IDs ergaenzen `manual_override_grid_ids` (manueller Auto-Close-Override); LoadDevice-Baseline gewinnt im selben Tick. §2.2 Registry-API: TickLoop-Konstruktor-Kwarg `agents: tuple[Agent, ...] = ()` + **Auto-Bus-Regel** (`agents != () and agent_bus is None` → leerer Bus, damit registrierte Agents nicht still als No-op enden) + Agent-ID-Eindeutigkeit (`AgentDuplicateIdError`); `build_tick_loop`-Symmetrie + `GridModelBilanz`-Overlay-Verdrahtung. §2.3 Lifecycle: `_attach_agents()` mit `set_run_id` + optionalem `_RandomAttachableAgent`-Sub-Protocol (`@runtime_checkable`); Sub-Random-Stream-Konvention `agent-{agent_id}` produktiv (Welle-3-Review-Folge M-3 geschlossen). §2.4 Bus-Eviction: `consume_for(receiver)` als **Direct-Inbox-destruktive** Drain-Variante; Broadcasts bleiben nicht-destruktiv (Welle-3-Review-Folge M-4 teilweise geschlossen; Broadcast-Watermark bleibt Folge-Slice). §2.5 Error-Family-Trennung: `AgentRegistryError`/`AgentDuplicateIdError` (Konstruktor), `AgentCommandDrainError(TickLoopError)`/`AgentInvalidCommandTargetError` (Schritt A0v), fuenf `TickLoopAgentSnapshot*Error` (Resume-Match); malformed `agent_bus`-Sub-Snapshots delegieren an `AgentBusSnapshot*`-Errors unwrapped. §2.6 Snapshot-Vertrag: `agent_bus` + `pending_agent_commands` als Sub-Snapshots; `TickLoop.from_snapshot(..., devices=..., agents=...)` mit Runtime-Dependency-Kwargs + Resume-Match-Checks (Device-ID/Typ/State, GridModel-State, LoadOverlay-State falls `grid_model`-Sub-Snapshot vorhanden, Pending-Command-Target-Validitaet); Auto-Bus-Praezedenz bei alten Snapshots ohne `agent_bus`; `CommandResult` als String-Roundtrip. **Konkrete Agent-Instanz-Snapshots (`agents.<type>.<id>`) bleiben Welle 4b**. Welle 4a entfernt Welle-3-`_set_agents_for_testing(...)`-Helper. Status-Uebergang: `Proposed → Provisional` mit M3-Welle-4a-C3-Closure `556baaf` (2026-05-21). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.6 Resume-Match-Checks geschaerft durch Welle-4a-Review-Folge (Commit `38272f6`): `_assert_device_resume_match` **bidirektional** (Subset-Restores werfen `TickLoopAgentSnapshotDeviceMismatchError`), nested-Equality via `canonical_json`-Bytes (kein False-Positive nach tuple/list-Roundtrip). §6 Welle-4b-Konsequenzen konkret geschaerft durch [`ADR 0027`](0027-rule-based-agent-scenario-pattern.md) (Schaerfung-ohne-Supersedes, ADR 0011-Pattern). |
| 0027 | [RuleBasedAgent + Scenario-Agents-Block-Pattern (M3 Welle 4b)](0027-rule-based-agent-scenario-pattern.md) | Provisional | 2026-05-22  | M3-Welle-4b-Erstwurf: Pattern-Pendant zu ADR 0025 (Welle-2-Konkretisierung) und ADR 0026 (Welle-4a-Foundation). Schaerft ADR 0026 §6 verbindliche Welle-4b-Konsequenzen ohne Supersede (ADR 0011-Pattern). Fuenf Entscheidungen: §2.1 `agents`-Schema als **nested Mapping** mit `agent_id` als Key (Schema-eindeutige IDs, fachliche Map-Semantik statt geordnete Liste; Konsistenz-Bruch zu `devices`/`faults`/`events`/`load_events`/`load_profiles` als bewusste fachliche Entscheidung dokumentiert); Loader iteriert `sorted(agents.keys())`. §2.2 `ScenarioAgent`-Domain (frozen dataclass, analog `ScenarioDevice`) + `_assert_agent_list(...)`-Validator (Pattern parallel zu `_assert_fault_list`); `_build_agents(...)` Factory-Dispatch analog `build_devices` + neue typed Errors (`ScenarioUnknownAgentTypeError`, `ScenarioUnknownAgentTargetError` u. a.). §2.3 `RuleBasedAgent`-**Hybrid Decision-Surface**: Default-Pfad ist deterministische Threshold-Rules-Liste (first-match-wins, geordnete Tuple, snapshot-bar, scenario-spezifizierbar); Erweiterungs-Pfad ist optionaler `plugin`-Hook mit `_AGENT_PLUGIN_FACTORIES`-Registry (Welle 4b leer; konkrete Plugins sind Welle 4c+); Mutual Exclusivity Rules ODER Plugin (vermeidet Reihenfolge-Drift). Neues `AgentPlugin`-Sub-Protocol (`@runtime_checkable`) mit eigenem `snapshot`/`from_snapshot`-Vertrag. §2.4 `agents.<agent_type>.<agent_id>`-Sub-Snapshot-Layout additiv per ADR 0015 §2.3 (kein Schema-Bump); bidirektionaler Resume-Match-Check analog Welle-4a-Review-Folge. §2.5 Welle-4-Abschluss-Gate: `make fullbuild` ohne Override cache-frei gruen mit Agent-Demo-Szenario. §2.6 Demo-Szenario-Location `tests/integration/scenarios/agents_demo.yaml` (Konsistenz zu `mvp_demo.yaml`/`fault_demo.yaml`). Status-Uebergang: `Proposed → Provisional` mit M3-Welle-4b-C2-Merge `95979e5` (2026-05-22) + C2-Review-Folge `11b2ca9` + dep-audit-Fix `ac7b47f` (starlette 1.0.0→1.0.1). `Accepted` mit M3-Welle-7-Closure. **Schaerfungen**: §2.2 build_tick_loop-`agents`-Kwarg-Default per Sentinel-Pattern (`None` statt `()` — Welle-4b-Review-Folge F-1, expliziter agentenloser Override wird respektiert). §2.3 + §7 Plugin-Restore-Scope-Schnitt expliziter: Welle-4b rekonstruiert nur Rules + plugin_name (kein Plugin-State); konkrete Plugin-Restore-Surface ist Welle-4c+-Material (Welle-4b-Review-Folge F-2). |
| 0028 | [Link-Maintenance an Accepted-ADR-`Bezug:`-Linien](0028-link-maintenance-accepted-adr-bezug.md) | Accepted    | 2026-05-23  | Schaerft `ADR 0006 §3` (Schaerfung-ohne-Supersedes per ADR 0011): Pfad-Updates an Markdown-Hyperlink-URLs in `Bezug:`-Linien sind keine fachliche Aenderung, sofern Ziel inhaltlich identisch bleibt, Linktext unveraendert/pfadnamen-spiegelnd ist und die Verweis-Menge gleich bleibt. Begruendung: Stub-Forwarder-Pattern skaliert nicht und steht im Widerspruch zur Positionsfragilitaets-Ratio aus ADR 0004 §1. Erstanwendung: Cleanup von 5 Forwarder-Stubs (`planning/in-progress/M1-tick-loop-spine.md`, `planning/in-progress/M2-devices.md`, `planning/open/003-random-port-adr.md`, `planning/open/012-snapshot-composition.md`, `planning/next/M1-tick-loop-spine.md`) + Pfad-Fixups in 13 Accepted-ADRs (0007, 0008, 0009, 0010, 0013–0021). |
| 0029 | [AC-NO-COVERAGE-PRAGMA als 11. tools/arch_check.py-Contract](0029-no-coverage-pragma-contract.md) | Accepted    | 2026-05-23  | Schaerfung von `ADR 0002 §A-1` (Schaerfung-ohne-Supersedes per ADR 0011): elfter `tools/arch_check.py`-Contract `AC-NO-COVERAGE-PRAGMA` verbietet `# pragma: no cover`, `# pragma: no branch` und `# pragma: exclude file` in `src/grid_gym/**`. Coverage-Gate-Disziplin: Pragmas verstecken sonst unzureichend getestete Pfade vor den `make coverage-gate`-/`coverage-gate-critical`-Schwellen. Protocol-Stubs werden ueber den `^\\s*\\.\\.\\.\\s*$`-Pattern in `[tool.coverage.report].exclude_lines` global ausgenommen (statt Pragma-Annotation pro Stub); defensives Dead-Code wird per `typing.cast` nach vorgelagerter Type-Validation ersetzt; `if TYPE_CHECKING:`-Bloecke sind bereits ueber den Standard-`exclude_lines`-Eintrag abgedeckt. Erstanwendung: 32 Pragma-Vorkommen aus `src/grid_gym/**` entfernt (29 Protocol-Stubs, 2 defensive `isinstance`-Guards in `validator.py`, 1 unreachable `return ()` in `loader.py`, 1 redundante TYPE_CHECKING-Annotation in `domain/scenario.py`). 17 A-1-Contracts insgesamt (6 import-linter + 11 arch_check). |
| 0030 | [DeviceProtocolPort-Surface (M4 Welle 1)](0030-device-protocol-port-surface.md) | Accepted    | 2026-06-01  | M4-Welle-1-Erstwurf (`Proposed` 2026-05-26 `b840e7a`) + Review-Folge `ad3dff8` (3H+4M+5L) + H4-Korrektur `111c464` (Decision 3 auf Caller-Scope — `TickLoop.run()` existiert nicht). Legt die `DeviceProtocolPort`-Surface (`GG-AR-PORT-DRN-007`) fuer die fuenf M4-Sub-Adapter MQTT/Modbus/OPC-UA/DNP3/IEC fest. Vier Decisions aus [`done/M4-welle-0.md`](../planning/done/M4-welle-0.md) §3 Decision-Liste: §2.1 Decision 2 **final** sync-`Protocol` mit Adapter-internem Event-Loop-Thread + Queue fuer async-Stacks (Welle 4 OPC-UA via `asyncua` traegt die Konstruktion zuerst real); §2.2 Decision 3 **final** expliziter Caller-Scope-Lifecycle via `TickLoop.start_protocol_ports()`/`stop_protocol_ports()` (FIFO-Start, LIFO-Stop, idempotent, Best-Effort-Partial-Cleanup mit `__context__`-Chain; Caller wrappt seine Tick-Schleife in `try/finally`; Replay-Mode skippt; Adapter-Lifetime == Run-Lifetime, nicht Service-Lifetime; **kein** `TickLoop.run()`); §2.3 Decision 7 **final** stateless aus Replay-Sicht, Reconnect-State volatile, Snapshot-Schema bleibt v2 in M4, reversibel via ADR-0015-Schema-Bump-Pfad; §2.4 Decision 1 **provisorisch** DNP3 + IEC-61850 Verzicht-Default, finale Disposition in M4-Welle 5 informiert durch asyncua-Erfahrung aus Welle 4 (§6 Verzicht-Anhang-Slot bleibt in Welle 1 leer; Welle 5 fuellt ihn entweder mit Verzicht-Text oder referenziert eigenen Spike-ADR). Welle 1 schreibt **keine** konkreten Adapter-Module — konkrete Implementer (`adapters/driven/protocol_mqtt/` etc.) kommen ab Welle 2. Status-Uebergang: `Proposed → Provisional` (M4-Welle-1-C3 `5f03bbf` + Linter-Folge `82f947c` nach C2-Merge `d09adf3` mit `device_protocol.py` + TickLoop-Lifecycle-Methoden + 23 neue Unit-Tests + `make arch-check`/`make gates` cache-frei gruen ohne Override; 19/19 Contracts KEPT = 7 lint-imports + 12 `tools/arch_check.py`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027). |
| 0031 | [MQTT-Adapter-Profile (M4 Welle 2)](0031-mqtt-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-2-Erstwurf (`Proposed` 2026-05-30 `4e102b8`) -> `Provisional` mit C3 (2026-05-30) nach C2-Merge `f33bb4e` (feat: `protocol_mqtt/` 7-Dateien-Modul + 50 neue Unit-Tests + Mosquitto-Integration-Smoke + `pyproject.toml`/`uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override). Vier Decisions aus [`done/M4-welle-0.md`](../planning/done/M4-welle-0.md) §3 Decision 4: §2.1 Decision 4a **final** Topic-Schema inline im `protocol_ports`-Scenario-YAML-Block (kein separater `mqtt_profiles`-Top-Level — Welle 2 spart die Indirektion ein; separate Section bleibt als Welle-6-Schaerfungspfad offen); §2.2 Decision 4b **final** Payload-Codec `canonical_json` (M2-Welle-0a-Stand, Trigger-014-Quelle: `done/014-generic-snapshot-format-codec.md`; Trigger 004 [`open/004`](../planning/open/004-canonical-encoder-alternative-adr.md) `canonical encoder`-Alternative bleibt mit Re-Eval-Pfad in Welle 6 — Aktivierungs-Bedingung: messbarer Perf-Druck am MQTT-Publish-Throughput im realen Compose-Smoke); §2.3 Decision 4c **final** QoS-Default `QoS 0` fuer Telemetry-Publishes (high-volume, Verlust-tolerant), `QoS 1` fuer Command-Publishes (acknowledged) und `QoS 1` fuer Subscribe-Pfade — pro `topics.<device_id>.qos_*` ueberschreibbar; §2.4 Decision 4d **final** Callback->Sync-Marshal via Per-Target `queue.Queue` (`dict[device_id, queue.Queue]`) mit Lazy-Init im paho-mqtt-`on_message`-Callback (laeuft im paho-Loop-Thread), `read(target)` macht `get_nowait()` (nicht-blockierend, `None` bei leer), `write(target, command)` ruft `client.publish()` direkt (thread-safe per paho-Doku); `on_message`-Exceptions werden geschluckt+geloggt (LogPort, falls vorhanden) und **nicht** an Loop-Thread propagiert. Welle 2 schreibt **keinen** OTel-Span-Wrap (Welle 6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done/M4-welle-1.md) §7). Status-Pfad: `Proposed → Provisional` (M4-Welle-2-C2-Merge nach feat-Commit mit `protocol_mqtt/`-Modul + 4 Unit-Test-Module + Integration-Smoke gegen Mosquitto-Sibling + `compose.yml`/`pyproject.toml`/`Dockerfile`-Edits + `make arch-check`/`make gates` gruen ohne Override) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030). |
| 0032 | [Modbus-TCP-Adapter-Profile (M4 Welle 3)](0032-modbus-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-3-Erstwurf (`Proposed` 2026-05-30 `a86ac46`) -> `Provisional` mit C3 (2026-05-30) nach C2-Merge `d721982` (feat: `protocol_modbus/`-5-Modul-Paket + 95 neue Unit-Tests + in-process pymodbus-Server-Integration-Smoke + `pyproject.toml`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override). Sechs Decisions: Register-Schema inline, Datatype-Set `{int16, uint16, int32, uint32, float32}`, direkt-sync, Function-Code-Mapping FC03/FC10 Defaults mit FC04/FC06-Overrides, Slave-Unit-ID per Target, in-process pymodbus-Server-Smoke. Review-Folge 2026-05-31: Smoke-Abdeckung auf Default-E2E-Profil praezisiert; [`done/031`](../planning/done/031-modbus-adapter-review-folge.md) hat FC06-Multi-Register-Guard, Read-/Write-Fehler-Taxonomie und bewusste Smoke-Abgrenzung umgesetzt. Trigger 006 [`open/006`](../planning/done/006-mypy-strict-bytes.md) ist nach positiver Re-Eval aktivierungs-reif; Aktivierung bleibt separater Folge-Slice. Welle 3 schreibt **keinen** OTel-Span-Wrap (Welle 6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done/M4-welle-1.md) §7). Status-Pfad: `Proposed → Provisional` (M4-Welle-3-C3-Merge nach C2 `d721982`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031). |
| 0033 | [OPC-UA-Adapter-Profile (M4 Welle 4)](0033-opcua-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-4-Erstwurf (`Proposed` 2026-05-31 `74ed35b`) -> `Provisional` mit C3 `7ad5baf` (2026-05-31) nach C2-Merge `78fdd7a` (feat: `protocol_opcua/`-6-Modul-Paket + 81 neue Unit-Tests + 8 in-process `asyncua.Server`-Integration-Smokes parametrisiert ueber alle 8 Datatypes + `pyproject.toml`-Pin auf `asyncua>=1.2b2,<2.0` (Beta wegen Python-3.14-Forward-Reference-Inkompat in 1.1.8) + mypy-Override `implicit_reexport=true` fuer `asyncua.*` + `uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Coverage 95.16 %, Branch-Critical-Coverage 90.95 %). **Erster rein-async-Stack** im Repo — ADR 0030 §2.1-Konsequenz produktiv vorgetragen via eigener `OpcuaLoopThread`-Klasse (dedizierter `asyncio.AbstractEventLoop` in `threading.Thread(daemon=True)` + `run_coroutine_threadsafe`-Marshal; normativer Teardown-Vertrag mit Lifecycle-Lock und Start-Timeout). Fuenf Decisions alle **final**: O-a Node-ID-Schema inline (numerisch + String, Pattern aus ADR 0031/0032); O-b Async-Bridge via Loop-Thread (ADR-0011-Schaerfungspfad fuer Welle-6 Schwester-Port-ADR offen); O-c Datatype-Set `{Boolean, Int16, UInt16, Int32, UInt32, Float, Double, String}` (8 Werte; weitere Welle-6-Schaerfung); O-d Polling-Read + Direct-Write (Subscription-Pfad als Welle-6-Forward-Pointer); O-e in-process `asyncua.Server` (LGPL-3.0; Pattern-Praezedenz Welle-3-M-f). **Slice-032-Schaerfung** 2026-05-31 ([`done/032`](../planning/done/032-opcua-adapter-review-folge.md)): 6 HIGH + 11 MEDIUM Code-Review-Findings umgesetzt — `threading.Lock` im `OpcuaLoopThread`, `start(timeout_s=)` + typed `OpcuaLoopThreadStartTimeoutError`, Port-Exception-Filter um `RuntimeError`/`CancelledError`, `Quality.INVALID`-Markierung fuer String-Reads, `Float`-32bit-Quantisierung im Codec, `OverflowError`-Catch, Pin-Range, Smoke-Server-`asyncio.Event`-Stop-Signal. ADR-Body geschaerft an §2.1 (Optional-Felder entfernt; YAGNI) und §2.5 (Test-Server-Loop bewusst getrennt von produktivem `OpcuaLoopThread`). Welle 4 schreibt **keinen** OTel-Span-Wrap (Welle 6), **keine** OPC-UA-Security (Anonymous-Endpoint; Welle 6 oder M6) und **keinen** Planted-Violator-Property-Test (Welle 6, Folge-Pflicht aus [`done/M4-welle-1.md`](../planning/done/M4-welle-1.md) §7). Folge-Pflicht: asyncua-Pin auf `>=1.2,<2.0` ziehen, sobald 1.2 final auf PyPI ist (Welle-6- oder M6-Material). Status-Pfad: `Proposed → Provisional` (M4-Welle-4-C3-Merge nach C2 `78fdd7a`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032). |
| 0034 | [DNP3-Adapter-Profile (M4 Welle 5a)](0034-dnp3-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-5a-Erstwurf (`Proposed` 2026-05-31 `b0fea7e`) -> `Provisional` mit C3 (2026-05-31) nach C2-Merge `224b370` (feat: `protocol_dnp3/`-5-Modul-Paket + 56 neue Unit-Tests inkl. hypothesis-Codec-Property-Tests + 4 in-process `dnp3-outstation.AsyncOutstation`-Integration-Smokes parametrisiert ueber alle 4 Group/Variation-Kombinationen + `pyproject.toml`-Pin `nfm-dnp3>=1.0,<2.0` produktiv + `dnp3-outstation>=0.2,<1.0` als dev-only Test-Sibling + mypy-Overrides `ignore_missing_imports=true` fuer `dnp3py.*`/`dnp3_outstation.*` + `uv.lock`/`Dockerfile`/`compose.yml`-Edits; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Tests 1462 Unit + 35 Integration). **Zwei-Library-Setup** (architektonisch neu in M4): Master/Client via `nfm-dnp3` 1.0.1 (PyPI, MIT, Pure-Python, sync API mit Thread-Lock-Schutz — C1-Probe-Run verifiziert) als Produktiv-Dependency; Outstation/Server via `dnp3-outstation` 0.2.0 (PyPI, MIT, Pure-Python, asyncio-native) **nur als Test-Sibling** in `[dependency-groups.dev]`. Welle 5a loest die in ADR 0030 §2.4 als „provisorisch" markierte DNP3-Verzicht-Disposition per Spike-Lieferung auf (Pattern ADR 0011; M4-Welle-7-Closure schaerft ADR 0030 §2.4 entsprechend). Fuenf Decisions alle **final**: §2.1 Decision D-a Point-Schema inline im `protocol_ports`-Block (Pattern-Praezedenz ADR 0031/0032/0033); §2.2 Decision D-b direkt-sync (Pattern-Praezedenz Welle-3-M-c — nfm-dnp3 ist sync, kein Loop-Thread noetig); §2.3 Decision D-c Group/Variation-Set `{Group 1/V1, Group 1/V2, Group 30/V1, Group 30/V5}` (Welle-5a-Minimum: Binary-Inputs + 32-bit Int/Float Analog-Inputs; Counter/Output/Event-Classes Welle-6-Schaerfung); §2.4 Decision D-d Class-0-Polling-Read mit Resultat-Filter-by-Index (DNP3-spec-konformes Idiom; `read_analog_inputs(start, stop)` verworfen wegen Wire-Compat-Limitation `dnp3-outstation` v0.2.0 supportet nur qualifier 0x00/0x06, nicht 0x01); §2.5 Decision D-e in-process `dnp3_outstation.AsyncOutstation` als Test-Sibling (Pattern-Praezedenz Welle-3-M-f + Welle-4-O-e). **C2-Library-Bug-Find**: `nfm-dnp3.AnalogInput.__repr__` zeigt `idx=0`, das tatsaechliche Attribut heisst aber `.index` — `_port._find_point` benutzt `getattr(point, "index", None)` ohne Wartepause. Welle 5a schreibt **keinen** DNP3-Write-Pfad (Welle 5b+, throws `Dnp3PortWriteNotImplementedError`), **kein** Event-Class-Polling (Welle 6), **keinen** OTel-Span-Wrap (Welle 6), **keine** DNP3-Security (Welle 6 oder M6) und **keinen** Planted-Violator-Property-Test (Welle 6). Status-Pfad: `Proposed → Provisional` (M4-Welle-5a-C3-Merge nach C2 `224b370`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032 + 0033). |
| 0035 | [IEC-61850-Adapter-Profile (M4 Welle 5b)](0035-iec61850-adapter-profile.md) | Accepted    | 2026-06-01  | M4-Welle-5b-Erstwurf (`Proposed` 2026-06-01 `88c1a33`) + C1-Review-Folge `da8aed9` (API-Korrektur read_value/write_value/Exception-Famille + Lizenz-Refit Optional-Extra statt [project] dependencies + M4-protocol-adapters.md-Sync nach 4 Findings) -> `Provisional` mit C3 (2026-06-01) nach C2-Merge `944bca5` (feat: `protocol_iec61850/`-5-Modul-Paket inkl. ImportError-Guard fuer Optional-Extra + 75 neue Unit-Tests inkl. hypothesis-Codec-Properties + Integration-Smoke unter **2c-Mock-only-Fallback aktiv** + NEU `LICENSES/GPL-3.0.txt` + LICENSE-Hinweis-Block + README+README.de-Lizenz-Sektion + SPDX-Header in 11 Files + NEU `tests/integration/fixtures/iec61850/simpleIO.cfg` libiec61850-natives CFG-Modell + `pyproject.toml`-Pin `pyiec61850-ng>=1.6,<2.0` in `[project.optional-dependencies.iec61850]` + mypy-Override `module="pyiec61850.*"` + `Dockerfile`-Edit `uv sync --extra iec61850` in deps/source-Stages + `CRITICAL_COV_TARGETS` um `adapters/driven/protocol_iec61850` + `compose.yml`-Header-Kommentar-Sync; `make arch-check` 19/19 + `make gates` cache-frei gruen ohne Override; Total-Tests 1537 Unit + 35 Integration passed + 4 IEC-Smokes skipped via `pytest.mark.skip` mit 2c-Fallback-Begruendung). **Erstmaliger Repo-Praezedenzfall** fuer GPL-isolierte Sub-Module in einem sonst MIT-Projekt (Decision I-f). Welle 5b loest den letzten ADR-0030-§2.4-Verzicht-Default (IEC-61850) per Spike-Lieferung auf (Pattern ADR 0011; M4-Welle-7-Closure schaerft ADR 0030 §2.4 auf „durch Welle-5a (DNP3) **und** Welle-5b (IEC-61850) aufgeloest"). Sechs Decisions alle **final**: §2.1 Decision I-a LN/CDC-Schema inline; §2.2 Decision I-b direkt-sync (Pattern-Praezedenz Welle-3-M-c + Welle-5a-D-b); §2.3 Decision I-c MMS-Datatype-Set `{bool, int32, float, string}` + FC-Mapping `{MX, ST, SP, CF, DC}` mit Adapter-Default `MX`; §2.4 Decision I-d Per-Target `MMSClient.read_value(reference, fc)`; §2.5 Decision I-e **2c-Mock-only-Fallback aktiviert** (Probe-Run auf Python 3.12 lief Float/Int32/String-Roundtrip mit `IedServer(model_path=fixture)` sauber durch, aber Docker-Stack Python 3.14 segfaultet im `_pyiec61850.so` SWIG-Layer mit exit 139 beim ersten `IedServer.start()` — pyiec61850-ng 1.6.1.2 manylinux1_x86_64-Wheel-ABI-Inkompat unter 3.14; Welle-6-Schaerfungspfade dokumentiert: Python-3.12-Runtime fixieren ODER Library-Upgrade ODER Wheel-Rebuild gegen 3.14); §2.6 **NEU Decision I-f Lizenz-Boundary** GPLv3-Isolation auf `protocol_iec61850/*` + zugehoerige Tests via SPDX-Header pro Datei + Optional-Extra-Setup; Rest grid-gym bleibt MIT. Welle 5b schreibt **keinen** Write-Pfad (Welle 6+, wirft `Iec61850PortWriteNotImplementedError`), **kein** RCB-Subscription (Welle 6), **kein** GOOSE-Publishing/Subscription (Welle 6), **kein** Sampled-Values (Welle 6 oder M6), **keinen** OTel-Span-Wrap (Welle 6), **keine** IEC-61850-Security (Welle 6 oder M6), **keinen** Planted-Violator-Property-Test (Welle 6) und **keine** aarch64-Wheel-Unterstuetzung (Welle 6 falls Bedarf). Status-Pfad: `Proposed → Provisional` (M4-Welle-5b-C3-Merge nach C2 `944bca5`) → `Accepted` (M4-Welle-7-Closure analog ADR 0022..0027 + 0030 + 0031 + 0032 + 0033 + 0034). **Slice 033** (M4-Welle-5b-C2-Review-Folge 2026-06-01, [`done/033`](../planning/done/033-iec61850-adapter-review-folge.md)): 15 Findings adressiert (10 HIGH + 5 MEDIUM) — Sentinel-Exception-Klasse statt `Exception`-Alias im Optional-Extra-Off-Pfad, `_PyIecMMSError`-Catch-All-Basis in `start()`, `stop()` State-Mutation NACH `disconnect()`, NaN/Inf-Reject + int-Reject in `_decode_float`, Container-Check gated auf non-string, NEU `Iec61850PortReadConnectionLostError` (mid-flight-NotConnected), Config-Anti-Scope-write-Reject bei Konstruktion, `TelemetryPoint.value`-Decimal-Wrap mit `Quality.INVALID`-String-Fallback (Welle-4-Slice-032-Pattern), Sub-Millisekunden-Timeout-Floor, `Dockerfile`-`build-app`-Stage `--extra iec61850`-Propagation, `simpleIO.cfg`-SPDX-Header + Derivative-Work-Attribution, `pyproject.toml`-GPL-Classifier ergaenzt. ADR-Status bleibt `Provisional`. |
| 0036 | [UI-Stack-Wahl (M5 Vorbelegung)](0036-ui-stack-choice.md) | Proposed    | 2026-06-01  | Pre-M5-Welle-0-Sondierungs-ADR (analog ADR 0030 fuer M4-Welle-1-Pattern). Listet drei realistische UI-Stack-Optionen plus eine ausgeschlossene vierte. **Option 1** FastAPI + HTMX + Jinja2 + Chart.js (Single-Stack-Python, 10 A-1-Gates, ~6 Wellen). **Option 1b** FastAPI-Backend + SvelteKit-SPA (Multi-Stack, 15 Gates, ~7-8 Wellen, UX-Glanz). **Option 2** FastAPI + React-SPA (strikt schlechter als 1b in allen Aspekten). **Option 3 ausgeschlossen** (Streamlit/Dash — eigener Server-Lifecycle, State-Mgmt nicht deterministisch, kein direkter WebSocket). Decision-Material vollstaendig; finale Auswahl offen. Status-Pfad: `Proposed` → `Provisional` (M5-Welle-1-ADR-Schaerfung) → `Accepted` (M5-Welle-7-Closure, analog ADR 0030..0035). |

---

## Lese-Reihenfolge bei Drift

Wenn Code, Tests oder Slice-Plaene auf einen Vertrag referenzieren,
der in einer aelteren `Accepted`-ADR steht, **immer pruefen, ob
eine Folge-ADR in der „Schaerfungen"-Spalte oben die Stelle
schaerft.** Im Zweifel:

1. Folge-ADR lesen — sie traegt die maßgebliche Fassung.
2. Original-ADR-Stelle bleibt historisch (kein Edit per
   `ADR 0006 §3`).
3. Adapter-/Modul-Code-Docstrings zitieren beide ADRs ueber den
   `ADR NNNN`-Tag (siehe z. B.
   `src/grid_gym/adapters/driven/random_mt/mersenne_twister.py`).

---

## Konvention

- Neuer ADR-Eintrag in dieser Tabelle ist Pflicht bei jeder neuen
  ADR. Reihenfolge: aufsteigend nach Nummer.
- Wenn eine ADR eine andere abloest oder schaerft, wird die
  „Schaerfungen"-Spalte der **alten** ADR aktualisiert. Die alte
  ADR selbst bleibt textlich unveraendert (per ADR 0006 §3).
- Statuswechsel (z. B. `Provisional → Accepted`) werden in der
  ADR-Datei selbst dokumentiert (Header-Pflichtfelder per
  `ADR 0006 §4`); diese Tabelle reflektiert sie.
